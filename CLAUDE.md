# CLAUDE.md — кореневий контекст для Claude Code

Ти працюєш у робочому просторі соло-засновника. Один репозиторій коду (ev-charge-bot) + бізнес-документація в `docs/`. Прочитай цей файл повністю перед будь-якою задачею.

## Робочий процес (автор/рев'ювер)

- **Sonnet 5 (ти, Claude Code)** імплементує → генерує `review_promptN.md` (повний дифф + тести).
- **Opus 4.8** незалежно рев'ює бандл ПЕРЕД PR/мержем (окрема сесія).
- Далі: PR → мерж → деплой на сервер (`git pull && docker compose build && docker compose up -d`) → жива перевірка в Telegram.
- **Спершу читай `docs/SESSION_STATE.md`** — актуальний стан і беклог.

## Поточний фокус (оновлювати вручну при зміні)

- **Активна гілка:** `main`.
- **Готово (у `main`):**
  - Промпти 1–5: тенанти/станції, еквайринг оператора, QR-флоу водія, кабінет оператора в Telegram, звірка й алерти (`reconcile_operators`).
  - **Monobank-еквайринг ЖИВИЙ** — реальні платежі йдуть напряму оператору-мерчанту; `mock_monobank.py` звірений з реальними формами банку (debit, і з 2026-07-29 — hold/finalize/cancel).
  - **OCPP 1.6J (3a/3b/3c-i):** Central System, транзакції+метринг, резервація kWh-балансу (модель A: резерв→факт→звільнення), бот-адмінкоманди `/ocpp_start`/`/ocpp_stop`, герметизація hold.
  - **Самообслуговуваний онбординг оператора:** автоактивація за перевіреними критеріями (профіль, токен банку підтверджений реальним зверненням, станція); межа публічної видимості `is_public` окремо від активації; suspend-кнопка.
  - Мертвий тестовий флоу «Банки» (`/webhook/monobank`) видалено — замінений живим Monobank Acquiring.
  - **Інфраструктура:** домен + HTTPS, порти БД/Redis/бота закриті назовні, секрети ротовані. Деталі — `docs/INFRA-AND-SECURITY.md`.
- **Зараз:** Модель B (3c-ii) задеплоєна і живо провалідована на проді 2026-07-30 (hold→finalize і hold→cancel, реальна картка, реальний вебхук). Наступне: малий бандл правок звірки (знахідки смоуку: processing-гонка кроку 1, невидимий алерт else-гілки кроку 4) і перший сторонній оператор.
- **Далі:** перший **сторонній оператор** (бізнес; пітчі готові).
- **Гейтовано юридично:** kWh-гаманець на СТОРОННІХ станціях (фаза 6b = платіжне посередництво) — див. `docs/billing-commission-models.md`. Фаза 6a (власні станції) — безпечна.
- **Заморожено до дня 60+:** AI-Продавець, P2P-лінія, мобільний застосунок.
- **Правило:** нічого нового не починати, поки поточне не в проді.
- Конкретні коміти/PR/Alembic head тут НЕ дублюються — актуальний стан завжди в `docs/SESSION_STATE.md`.

## Технічні конвенції (обовʼязкові, деталі — docs/PROJECT_CONTEXT.md)

- Python 3.11 · FastAPI · aiogram 3.x · asyncpg із **сирим SQL без ORM** · Alembic
- Єдині екземпляри `bot`/`dp` — з core loader; нових не створювати
- Кожна Alembic-міграція дублюється idempotent-блоком у `create_tables()`/`init_operator_tables()` — оновлювати ОБИДВА місця (тестується)
- Будь-яка зміна балансу користувача — ТІЛЬКИ через `update_user_balance()`; ніколи напряму в баланс чи журнал транзакцій
- Мультитенантність білінгу: кожна таблиця і кожен запит — з `operator_id`; тести ізоляції тенантів обовʼязкові (`test_operator_isolation.py`, `TENANT_SCOPED_CALLS`)
- Будь-який текст оператора з `parse_mode="HTML"` — через `html.escape()`
- Monobank-вебхук: не довіряти тілу — re-verify через `get_invoice_status` токеном оператора; невідомий invoiceId → тихий 200
- Ідемпотентність оплат: партк. unique-індекс `uq_ledger_session_income` + mutex `UPDATE ... WHERE status <> ...` — перевикористовувати, не переписувати
- `ENCRYPTION_KEY` (Fernet) шифрує токен оператора в БД — НЕ міняти без перешифрування
- Секрети — тільки `.env` (НЕ в git); ніколи не комітити, не логувати
- Тести — на фейкових зʼєднаннях без живої БД, у стилі існуючих; лишаються зеленими
- Прод (DigitalOcean droplet) агентом напряму не чіпати: локальна розробка → git push → деплой руками

## Карта документації (docs/)

| Файл | Що всередині | Коли читати |
|---|---|---|
| `SESSION_STATE.md` | Актуальний стан і беклог | **Першим, перед будь-якою задачею** |
| `PROJECT_CONTEXT.md` | Повний технічний контекст кодової бази | Перед будь-яким кодом |
| `INFRA-AND-SECURITY.md` | Домен, HTTPS, порти, секрети, процедури ротації | Інфра/безпека/деплой |
| `billing-commission-models.md` | Механізм 5% (моделі A/B/C: інвойс / Monobank Split / уникати посередництва) | Питання комісії/розрахунків |
| `ocpp-and-wallet-plan.md` | OCPP (Промпт 3) + kWh-гаманець: рішення, фазування, послідовність | Задачі OCPP/гаманця |
| `kwh-wallet-design.md` | Дизайн kWh-гаманця (повна модель, фази 6a/6b/6c) | Задачі гаманця |
| `plan-90-dniv-pokrokovo.md` | Операційний план по днях, DoD фаз, kill-критерії | Планування тижня/дня |
| `evolt-white-label-bilinh-ta-p2p.md` | Головна стратегія: білінг + промпти, карта-агрегатор, P2P, застосунок | Задачі по eVolt |
| `fop-podatkovyi-asystent.md` | ФОП-асистент | Задачі по ФОП |
| `ai-prodavets-dlia-magazyniv.md` | AI-продавець | Заморожено; тільки за прямим запитом |
| `spilna-infrastruktura-server.md` | Спільний сервер, інфра | Інфраструктурні задачі |

## Правила виконання задач

1. Один промпт = один модуль; після модуля — інструкція запуску + тести. Не генерувати «все одразу»; великі фічі (OCPP) різати на під-бандли (3a/3b/3c)
2. Платіжна логіка, міграції, баланси — пояснюй план ДО написання коду і познач місця для ручного рев'ю; результат іде на рев'ю Opus 4.8 як `review_promptN.md`
3. Якщо задача суперечить конвенціям вище — зупинись і спитай, а не обходь
4. Дані/цифри в бізнес-файлах — оцінки; не сприймай як факти про ринок без перевірки

## Бізнес-контекст одним абзацом

Соло-засновник, повний фокус, бюджет ~0 → шлях: пілоти → перші платні оператори → реінвест. Довга траєкторія: білінг → smart charging → агрегація гнучкості (VPP). **Комісія платформи: 5%, без абонплати** (рішення 2026-07-23, на старт; попередня згадка «590 грн/міс + 3–5%» скасована). Деталі — у відповідних файлах docs/.
