# Стан роботи — передача контексту між сесіями

> Короткий зріз «де ми зараз» для наступної сесії та для зовнішнього рев'юера.
> **Оновлювати наприкінці кожної робочої сесії.** Довгий технічний контекст —
> `PROJECT_CONTEXT.md`, конвенції — `CLAUDE.md`, стратегія —
> `evolt-white-label-bilinh-ta-p2p.md`.
>
> Останнє оновлення: **2026-08-03**

## Де ми

| | |
|---|---|
| Гілка | `main` — усе нижче вже в `main`: самообслуговуваний онбординг оператора (автоактивація за 3 критеріями, чек-лист, verify-token живим зверненням до банку, suspend-кнопка), OCPP 3a/3b/3c-i, IN-PROCESS адмін-тригер (`/ocpp_start`/`/ocpp_stop`), герметизація hold, `mock_monobank.py` навчений hold-формам, і тепер **Модель B (3c-ii) — гривневий hold через Monobank**. **ЗАДЕПЛОЄНО на прод 2026-07-29 ~20:50 UTC** (Alembic 0018), **живий смоук самої Моделі B (не лише мока) ПРОЙДЕНО наскрізно на проді 2026-07-30** — hold→finalize і hold→cancel через реальний вебхук і production-код, реальні гроші, гейт беклогу закрито (див. «Зроблено») |
| Останній коміт у `main` (код) | `732d8d8` `fix(api): disable public Swagger/OpenAPI docs by default` — змержено rebase'ом. **ЗАДЕПЛОЄНО на прод 03.08.2026**, без міграцій. Перед ним — `975e0ea` `fix(operator): verify token before save, suspended-aware menu, cabinet UX` (**ЗАДЕПЛОЄНО 2026-08-02 ~21:13 UTC**); перед ним — `aa5e34f` `chore(security): захист вiд секретiв у комiтах` (бар'єр проти секретів у комітах, деплою не потребував — у контейнер не входить); перед ним — `5f5dfc0` `feat(search): адаптивний радіус TomTom 15→30 км і окремі картки за замовчуванням`. **Прод — на `732d8d8`**: код на проді і код цього коміту збігаються, дрейфу немає. Деталі — розділ «Зроблено», **«Закриття публічного Swagger/OpenAPI (03.08.2026)»** |
| PR (гілка `feature/model-b-uah-hold`, коміт `bf35ae1`→`main` `3b2d401`) | змержено й **ЗАДЕПЛОЄНО 2026-07-29 ~20:50 UTC** (Модель B (3c-ii) — гривневий hold через Monobank: міграція `0018`, нові репо-функції, `finalize_invoice()`/`cancel_invoice()` у клієнті банку, `app/services/ocpp_charging.py` uah-оркестрація, гілка в `on_stop_transaction`, новий вебхук `/webhook/charging-hold/{operator_id}`, адмінкоманда `/ocpp_start_uah`, чотири кроки звірки; три раунди рев'ю плану ДО коду + три раунди рев'ю коду, два грошові блокери знайдено й закрито, третій раунд — без зауважень; один коміт на весь бандл (міграція і код, що на ній працює, — нероздільні); 697 passed, 7 skipped; див. «Зроблено») |
| PR #32 (гілка `feature/self-service-onboarding`, коміт `d8d408b`) | змержено й **ЗАДЕПЛОЄНО 2026-07-28** (pending→active автоматично за 3 критеріями — профіль/токен Monobank підтверджений банком/станція; `is_public` окрема ручна межа публічності; suspend-кнопка; `commission_pct` DEFAULT 4→5 для нових операторів; рев'ю Opus, 3 правки застосовано; живий смоук на проді того ж дня — див. «Зроблено») |
| PR (гілка `feature/ocpp-hold-hardening`, коміт `7bd3bac`) | змержено й **ЗАДЕПЛОЄНО 2026-07-25** (герметизація hold у `start_charging_session()` — `try/except BaseException` + `asyncio.shield()`; UX-обгортка бот-хендлерів `/ocpp_start`/`/ocpp_stop`; документація крона `reconcile_charging_reservations.py`; рев'ю Opus, 3 правки застосовано; див. «Зроблено») |
| PR (гілка `feature/ocpp-admin-commands`, коміт `3978b10`) | змержено й **ЗАДЕПЛОЄНО** (бот-адмінкоманди `/ocpp_start`/`/ocpp_stop` — IN-PROCESS RemoteStart/RemoteStop; закриває знахідку «3c-i незапускний на проді», див. «Зроблено»; рев'ю Opus, одна правка — адмін-гейт) |
| PR #27 | змержено (OCPP 3c-i — резервація kWh-балансу, модель A: резерв→факт→звільнення; міграції 0015/0016; рев'ю Opus) |
| PR #26 | змержено (CI job `live-db-tests` з реальним Postgres) |
| PR #25 | змержено (хотфікс `start_ocpp_transaction` — writable-CTE не проставляв `ocpp_transaction_id` на реальному PG; фікс: 2 кроки в `conn.transaction()`; рев'ю Opus + real-PG регресійний тест) |
| PR #24 | змержено (OCPP 3b — транзакції+метринг: Authorize/Start/Stop/MeterValues + RemoteStart/Stop, міграція 0014; рев'ю Opus пройдено) |
| PR #23 | змержено (OCPP 3a — кістяк Central System; рев'ю Opus пройдено, латентний registry-eviction закрито) |
| PR #22 | змержено (wallet-realmono — реальні Monobank-платежі за kWh-пакети; рев'ю Opus, грошовий блокер атомарності знайдено й закрито) |
| PR #17 | змержено (мок vs реальність — `mock_monobank.py` звірено з живою відповіддю банку) |
| PR #16 | змержено (Промпт 5 — `reconcile_operators.py`) |
| PR #15 | змержено (Промпт 4c — станції операторів у водійському пошуку) |
| PR #14 | змержено, рев'ю пройдено (міні-фіча — активація оператора кнопкою) |
| PR #13 | змержено (Промпт 4 — кабінет оператора) |
| PR #11 | змержено (Промпт 2 + `CLAUDE.md`/`docs` у `main`) |
| PR #10 | змержено (Промпт 1) |
| Alembic head | `0018_charging_reservations_uah` у `main`, **ЗАДЕПЛОЄНО на прод 2026-07-29 ~20:50 UTC** (`alembic upgrade` 0017→0018 підтверджено логом деплою; схема звірена наживо через `\d charging_reservations`: рівно 4 CHECK-обмеження — старий `payment_method`-CHECK не лишився дублем, 4 нові колонки (`reserved_uah`/`invoice_id`/`final_amount_uah`/`driver_contact`), `user_id`/`reserved_kwh` стали nullable, частковий унікальний індекс на `invoice_id` є). Попередній стан: `0017_operator_self_service`, задеплоєно 2026-07-28 |
| Тести | **886 passed, 7 skipped** у `main` (+2 бандлом закриття публічного Swagger 03.08.2026 — `test_api_docs.py`, окремі процеси на сценарій, деталі → «Зроблено»; до цього +9 бандлом операторського UX 02-03.08.2026 — `test_operator_cabinet.py` окремо 112; ще раніше +94 у `test_check_secrets.py` — бар'єр проти секретів, 01.08.2026; +3 real-PG у `live-db-tests`, потребують `DB_URL`) |
| Задеплоєно на прод | **так** (2026-07-21, Промпт 4c докочений 2026-07-21 увечері й перевірений наживо) — `ENCRYPTION_KEY`, `PUBLIC_BASE_URL` у `.env`, WARNING зник, `/health` відповідає. **Код Промпту 5 на проді** (перезбірки 2026-07-23 після мержів; рефакторинг вебхука `apply_bank_status`/`complete_paid_session` + `reconcile_operators.py` на сервері), звірка перевірена наживо, **cron щогодини додано 2026-07-23**. **wallet-realmono (buy-side Monobank) задеплоєно 2026-07-23** — `WALLET_OPERATOR_ID=1`, `BOT_USERNAME` у `.env`, `wallet_topups` створено, вебхук `/webhook/wallet/1` перевірено наживо. **OCPP 3a/3b/3c-i + бот-адмінкоманди `/ocpp_start`/`/ocpp_stop` — ЗАДЕПЛОЄНО 2026-07-24**: Alembic 0011→0016; nginx `/ocpp` WS-проксі готовий (`location /ocpp`, `Upgrade`/`Connection`, timeout 3600s); OCPP 3a підтверджено живим `wss://`-смоуком (Boot/Status/Heartbeat); повна жива валідація 3b/3c-i (модель A) через `/ocpp_start`/`/ocpp_stop` з лог-супергрупи — див. «Зроблено». **Герметизація hold — ЗАДЕПЛОЄНО 2026-07-25** (коміт `7bd3bac`), крон `reconcile_charging_reservations.py` налаштований (`30 * * * *`), живий смоук `/ocpp_start`→`/ocpp_stop` ПІСЛЯ цього деплою проведено того ж дня (два послідовні цикли, станція `SMOKE-3C-2`, див. «Зроблено»). **Самообслуговуваний онбординг оператора — ЗАДЕПЛОЄНО 2026-07-28** (PR #32, коміт `d8d408b`): Alembic 0016→0017; живий смоук того ж дня — pending→невалідний токен(401)→валідний токен(200)→автоактивація на додаванні станції, межа `is_public` доведена контрольним експериментом, suspend і прибирання даних перевірені наживо (див. «Зроблено»). **Модель B (3c-ii) — ЗАДЕПЛОЄНО 2026-07-29 ~20:50 UTC** (коміт `3b2d401`): Alembic 0017→0018 підтверджено логом деплою; схема перевірена наживо через `\d charging_reservations` (4 CHECK, 4 нові колонки, `user_id`/`reserved_kwh` nullable, частковий унікальний індекс на `invoice_id`); крон `reconcile_charging_reservations.py` — 5 прогонів 00:30–04:30 UTC, усі 0/0, без `Unclosed`-шуму. **Живий смоук самого hold→finalize/cancel через Модель B ПРОЙДЕНО 2026-07-30** — наскрізно через реальний вебхук і production-код, реальні гроші (див. «Зроблено»). **Карта TomTom — ЗАДЕПЛОЄНО 2026-07-31 чотирма послідовними бандлами**: `feature/tomtom-live-layer` (живий інфошар TomTom у водійському пошуку), `fix/merge-ocm-tomtom-layers` (об'єднання джерел + дедуплікація замість exclusive-or, приглушення витоку ключа TomTom у логи httpx), `feature/search-results-single-message` (квота видачі 4 DC + 2 AC замість зрізу за відстанню, одноповідомленнєвий рендер за прапорцем), `feature/adaptive-radius-and-cards` (адаптивний радіус TomTom 15→30 км, дефолт рендеру повернуто на окремі картки рішенням власника). Юридична рамка TomTom, виміряне покриття України й знахідки живого смоуку — розділ «Зроблено», **«Карта: живий інфошар TomTom (31.07.2026)»**. **Операторський UX перед першим стороннім оператором — ЗАДЕПЛОЄНО 2026-08-02 ~21:13 UTC** (коміт `975e0ea`): без міграцій, `alembic upgrade` не потрібен; три контейнери піднялись `healthy`, лог старту чистий. Живий смоук на проді того ж дня — знімок рядка `operators` id=1 до/після надсилання неправильного токена, значення збіглися (див. «Зроблено»). |
| Перший живий платіж | **так, 2026-07-22** — сесія #1, 20 грн, Monobank Acquiring, `status=success`. Критичний шлях (див. «Зовнішні фронти») розблоковано |

Наступну роботу починати новою гілкою від `origin/main`.

## Фокус і рішення (2026-07-23)

**Поточний фокус (за пріоритетом):**

1. ~~**Реальні продажі kWh-пакетів через Monobank-еквайринг**~~ — **ЗРОБЛЕНО й задеплоєно 2026-07-23 (PR #22)**. Buy-side пакетів тепер через живий Monobank-еквайринг оператора №0; окрема таблиця `wallet_topups` + окремий роут `/webhook/wallet/{operator_id}`; розрізнення поповнення vs станційна оплата — на рівні роутингу (окремий webhook_url), а не парсингом `reference`; баланс лише через `update_user_balance`, ідемпотентно й **атомарно** (мʼютекс+кредит в одній `conn.transaction()`). **Наступний фокус — тепер OCPP (п.2).**
2. **OCPP (Промпт 3, фази 3a/3b/3c)** — **3a (кістяк) і 3b (транзакції+метринг) ЗРОБЛЕНО й змержено (PR #23, #24), рев'ю Opus пройдено обидва.** 3a: WS `/ocpp/{cp_id}`, Boot/Heartbeat/Status, basic auth. 3b: Authorize/Start/StopTransaction/MeterValues + RemoteStart/StopTransaction, kWh=meterStop−meterStart у сесію, ідемпотентність ретраїв, міграція 0014. **Наступне — 3c:** інтеграція оплати/гаманця на станції (spend-side) — тригер RemoteStart від оплати/балансу, ціна за фактом після StopTransaction, списання kWh-балансу АБО грн (Monobank hold+capture). Стек: `ocpp` (mobilityhouse) 1.6J, WS у FastAPI, симулятор. Інфра: nginx WS-проксі на `/ocpp` — **зроблено й задеплоєно 2026-07-24** (див. «Де ми»/«Зроблено» нижче — актуальніші за цей історичний абзац 2026-07-23).
3. **Перший сторонній оператор** (бізнес). Пітчі готові (дрібні + мережі, 5% комісія).

**Ключові рішення:**

- **Механізм комісії 5%** (`billing-commission-models.md`): **Модель A** (щомісячний інвойс/звір на поточній архітектурі) — зараз; **Модель B** (Monobank Split, автозбір, банк ділить у джерелі) — на масштаб; **Модель C** (збирати все на рахунок eVolt і виплачувати) — УНИКАТИ (платіжне посередництво: регуляція НБУ, податки на весь оборот).
- **kWh-гаманець:** будуємо **фазу 6a — лише на ВЛАСНИХ станціях eVolt** (юридично безпечно). Сторонні станції (6b) = Модель C = юридичний гейт. `KWH_RATE_UAH = 15` (тимчасовий, єдина константа, показ водію до підтвердження).
- **Повна модель гаманця** (вибір кВт/грн, ціна за фактом спожитого ПІСЛЯ сесії) **вимагає метрингу → OCPP (Промпт 3)**. Тупі розетки (Schuko) енергетичний білінг не вміють (не вимірюють спожите, не зупиняються на N кВт·год) — лише фікс за сеанс. Тому OCPP — передумова повного гаманця.

**Стан гаманця (buy-side):** купівля пакетів (50/100 кВт·год = 750/1350 грн) — **тепер через ЖИВИЙ Monobank-еквайринг** (PR #22, задеплоєно 2026-07-23). Старий тестовий Telegram Payments лишається за фіче-прапорцем `TELEGRAM_PAYMENTS_ENABLED` (default off). Баланс у кВт, ваучер, «Мій білінг» — є. Витрати балансу на станції (spend-side) — **немає** (це 3c/OCPP).

**Модель комісії (рішення 2026-07-23):** **5%, без абонплати**, на старт. Попередня згадка «590 грн/міс + 3–5%» скасована — лишилось вирівняти в стратегічному `evolt-white-label-bilinh-ta-p2p.md`, якщо там ще є.

**Нова документація:** `INFRA-AND-SECURITY.md`, `billing-commission-models.md`, `ocpp-and-wallet-plan.md`; `CLAUDE.md` освіжено (фокус + карта доків).

## Зроблено

**Промпт 1 — тенанти і станції** (міграція `0010`, 74 тести в `test_operator_isolation.py`)

- `operators`, `operator_stations`, `operator_sessions`, `operator_payout_ledger` — у кожної `operator_id`
- Ізоляція тримається на трьох механізмах: `operator_id` першим аргументом і у `WHERE` кожної функції; `create_session` бере `operator_id` з підзапиту по станції, а не з аргументу; композитний FK `(station_id, operator_id)`
- Балансу оператора як колонки немає — `SUM` журналу, щоб не повторити розсинхрон `users.balance` ↔ `kw_transactions`
- Комісія в `Decimal` з `ROUND_HALF_UP` (`round()` по float зрізав копійку: 112.5 × 5% давало 5.62 замість 5.63)
- Ідемпотентність нарахувань — частковий унікальний індекс `uq_ledger_session_income`

**Промпт 2a — еквайринг оператора** (міграція `0011`, 32 тести в `test_operator_payments.py`)

- Інвойс створюється токеном мерчанта-**оператора**; гроші йдуть напряму йому, ми лише софт
- **Webhook не вірить власному тілу** — статус перепитується в банку токеном оператора. Причина: інвойси різних мерчантів підписані різними ключами, тож глобальна ECDSA-перевірка з `app/api/payments.py` для них не працює в принципі
- Невідомий `invoiceId` → тихий 200 без подробиць, щоб ендпоінт не став оракулом
- Токени операторів шифруються Fernet на `ENCRYPTION_KEY`; без ключа застосунок стартує, але видає гучний WARNING
- Окрема таблиця `operator_payments`, бо водій не реєструється, а `payments.user_id` — `NOT NULL`

**Промпт 2b — QR-флоу водія** (43 тести в `test_driver_qr.py`)

- `GET /s/{qr_slug}` → вибір суми → `POST /start` → інвойс → редірект у Monobank → webhook → пуш оператору «Оплачено, увімкніть станцію» → кнопка → сесія `charging`
- Водій не реєструється: єдиний ключ — `qr_slug`. Чек звіряє, що сесія належить саме цій станції
- Сторінки без жодного зовнішнього ресурсу (водій буває в підземному паркінгу); оновлення чека через `meta refresh`, не JS
- Оплату приймає лише оператор зі статусом `active`

**Промпт 4 — кабінет оператора в Telegram** (без нової міграції, 56 тестів у `test_operator_cabinet.py` + 2 нові тенант-скоупні функції в `test_operator_isolation.py`; +7 тестів і кнопка активації з міні-фічі нижче)

- Вхід — `/operator` або кнопка «🏷️ Мій білінг»: новий Telegram-акаунт -> онбординг (назва, телефон) -> `operators` зі статусом `pending`, сповіщення в `LOGS_CHAT_ID` з кнопкою «✅ Активувати»; автоактивації немає, пряма ручна активація через `set_operator_status()` лишається робочою
- **Міні-фіча «активація кнопкою»** (окремий PR, `feature/operator-activation`): кнопка під сповіщенням про нового оператора переводить його в `active` і шле йому «🎉 Ваш кабінет активовано!». Захист — звірка `int(callback.message.chat.id) == int(LOGS_CHAT_ID)` (єдиний свідомий виняток із правила «operator_id лише з telegram-акаунта», бо це адмінська дія без «свого» оператора). Ідемпотентно (повторний клік -> «Уже активовано», без другого сповіщення); збій сповіщення оператору (заблокував бота) не скасовує саму активацію
- Підключення еквайрингу, майстер станції, тарифи/статус і виручка доступні вже під час `pending` — оплати від цього не залежать, їх окремо блокує перевірка статусу оператора в `app/api/driver_qr.py`
- Токен Monobank: лише приватний чат, шифрування одразу (`encrypt_secret`), `bot.delete_message` на повідомлення з токеном, підтвердження показує лише останні 4 символи
- Майстер станції: назва → адреса → конектор → потужність → тариф → плата за старт; опційні поля пропускаються через «-»; QR-PNG (`qrcode[pil]`, новий рядок у `requirements.txt`) генерується й одразу надсилається в чат
- Нові callback_data (`opm:`/`opst:`/`oprev:`/`opcsv:`) свідомо не несуть `operator_id` всередині — лише `station_id`/період; тенант завжди береться з `get_operator_by_telegram_id(from_user.id)` того, хто натиснув
- Виручка сьогодні/тиждень/місяць — `SUM` з `operator_payout_ledger` за типами (нові `get_ledger_summary`/`list_ledger_since`), CSV-експорт у чат
- **Архітектурна правка:** `operator_billing_router` переміщено ПЕРЕД `user_router` в `app/main.py` — інакше приймач-заглушка "будь-який текст без `/` -> ШІ-чат" з `user.py` ковтав би кожен вільнотекстовий крок майстрів кабінету раніше, ніж вони доходили б до свого хендлера. Кожен такий крок додатково виключає повідомлення, що починаються з `/`, щоб `/start` завжди міг вивести з майстра

**Промпт 4c — станції операторів у водійському пошуку** (без нової міграції — `lat`/`lng` уже були в схемі; нові файли `test_geo.py`, `test_station_speed.py`, `test_user_station_search.py`, +9 тестів у `test_operator_cabinet.py`, +3 у `test_operator_isolation.py`)

- Майстер станції: після адреси — крок геолокації (кнопка 📍, текст «lat, lng», або «-» з попередженням, що станція не потрапить у пошук), потім крок конектора інлайн-кнопками (`Type 2 · GB/T AC · Schuko · CEE 5-pin (3ф) · Інше → вільний текст`) замість вільного тексту — конектор фактично став обов'язковим (без «Пропустити»), бо саме він разом із потужністю формує бейдж швидкості
- `list_public_stations_near(lat, lng, radius_km)` (`app/database/operators_repo.py`) — **третій** свідомий публічний виняток із правила ізоляції (після `get_station_by_qr_slug` і `get_station_by_ocpp_charge_point_id`): активні станції активних операторів із координатами, SQL фільтрує статуси й `NULL`-координати, Python (`haversine_km`, `app/services/geo.py`) — радіус і сортування за відстанню
- `classify_station_speed(power_kw, connector_type)` (`app/services/station_speed.py`) — однакова чиста функція для операторських станцій і OCM: якщо потужність відома — рішення лише за нею (⚡ ≥50 кВт, 🔌 22–50, 🐢 ≤22); якщо ні — за конектором (CCS/CHAdeMO/GB/T DC -> швидка, Schuko/CEE/розетка -> повільна); якщо і конектора нема — без бейджа, не вгадуємо
- `app/services/ocm_service.py` тепер додатково повертає `power_kw`/`connector_type` (найпотужніший конектор станції) — потрібно для того самого бейджа на OCM-станціях
- Пошук водія (`handle_location`, `app/handlers/user.py`) об'єднує OCM і операторську видачу, сортує за відстанню (`_merge_search_results` — чиста функція); картка операторської станції веде на `/s/{qr_slug}` (оплата через QR), картка OCM лишається як була, з бейджем спереду
- Фільтрів «швидкі/повільні» у видачі свідомо немає — у результаті 3–5 станцій, бейджів достатньо (беклог, п. 6 нижче)
- **За результатом рев'ю:** `html.escape()` на всіх полях, які вводить оператор вільним текстом (назва станції, адреса, конектор, назва оператора), у кожному місці рендеру з `parse_mode="HTML"` — `_format_operator_station_card`, `_station_detail_text`, `_send_cabinet_home`, підпис у `_send_new_station_qr`, `_notify_admins_new_operator`. Без цього символ `<` у назві ламав парсинг HTML (Telegram узагалі не надсилає повідомлення) — найгірший випадок саме в сповіщенні адміну: заявка оператора вже записана в `operators`, але адмін ніколи про неї не дізнається, бо `bot.send_message` падає з винятком. Другий ризик — лінк-ін'єкція (`<a href=...>`) у публічній видачі водіям

**Промпт 5 — звірка операторського білінгу й алерти** (без нової міграції; новий `reconcile_operators.py`, +4 тенант-скоупні функції в `operators_repo.py`, +4 у `TENANT_SCOPED_CALLS` (`test_operator_isolation.py`), новий `test_reconcile_operators.py`, 24 тести)

- Закриває обидва свідомо відкладені вікна Промптів 2a/2b (див. таблицю нижче): вебхук рефакторено — спільна логіка застосування відповіді банку (`apply_bank_status`) і завершення оплаченої сесії (`complete_paid_session`) винесена в `app/api/operator_webhook.py` і використовується і webhook'ом, і звіркою, щоб платіжна логіка не дублювалась у двох місцях. Рефакторинг поведінково ідентичний — увесь `test_operator_payments.py` (32 тести) пройшов без жодної зміни
- 4 сценарії розбіжностей: (1) pending-платежі старші за `INVOICE_TTL_SECONDS` — перепитуються банком і фіналізуються тим самим шляхом, що й webhook; (2) success-платежі без доходу в журналі — доначислюються ідемпотентним `complete_paid_session`; (3) success-платежі без сесії — лише алерт (нараховувати нікуди); (4) pending-сесії без `payment_id` старші за годину — лише алерт (invoice_id невідомий)
- Новий `list_operators()` (`operators_repo.py`) — **четвертий** свідомий тенант-нескоупний виняток із правила ізоляції: звірка за природою мусить пройти по всіх операторах; секретів не повертає
- Один зламаний/недоступний токен оператора не зупиняє звірку решти — оператор пропускається з алертом (`stats.bank_unavailable`), обробка інших операторів продовжується
- Ідемпотентність перевірена явним тестом (`test_second_run_does_not_double_credit_anything`) — два прогони поспіль дають той самий журнал і те саме число сповіщень
- Підсумок (перевірено/виправлено/потребує рук) друкується в консоль і, якщо задано `LOGS_CHAT_ID`, шлеться туди ж; отримання `bot` винесено в окрему функцію `_get_bot()` саме заради тестованості — збій пуша логується, але не ламає звірку і не впливає на exit code
- Exit code 1, якщо є пункти на ручний розбір або банк був недоступний для когось з операторів — той самий cron-контракт, що й у `reconcile_payments.py`

**Мок vs реальність — звірка `mock_monobank.py`** (без нової міграції й без нових тестів — жодної розбіжності рівня бага не знайдено, `fix/mock-vs-reality`)

- Перший живий платіж (2026-07-22, сесія #1, 20 грн) дав реальний payload `invoice/status`. Наш код читає з нього лише `status`/`amount` (`app/api/operator_webhook.py`) — обидва поля вже збігались з `mock_monobank.py` й формою, і типом (копійки, `int`). **Розбіжностей, що впливають на код, не знайдено.**
- Мок доповнено полями, які реально повертає банк, але яких мок не знав: `payMethod`, `createdDate`, `destination` (раніше губилось — тіло запиту приймалось, але нікуди не зберігалось), `finalAmount`, `paymentInfo` (вкладений об'єкт: `fee`/`rrn`/`bank`/`tranId`/`country`/`terminal`/`maskedPan`/`approvalCode`/`paymentMethod`/`paymentSystem`), `modifiedDate` — форма як у банку, значення вигадані заглушки (не справжні реквізити з проду)
- **Не перевірено:** форма відповіді для `failure`/`expired`/`reversed` — живих зразків цих статусів ще немає, тому мок для них навмисно НЕ додає `paymentInfo`/`finalAmount`/`payMethod` (консервативно, щоб не видавати здогад за факт). Звірити, коли трапиться перший живий провал оплати

**wallet-realmono — реальні продажі kWh-пакетів через Monobank** (міграція `0012_wallet_topups`; +36 тестів: `test_wallet_topup.py` 24 / `test_wallet_topup_purchase.py` 13; PR #22, задеплоєно 2026-07-23)

- Кнопки «Пакет 50/100 кВт·год» тепер створюють реальний Monobank-інвойс токеном оператора №0 (`WALLET_OPERATOR_ID`, env — не хардкод), а не тестовий `bot.send_invoice`. Старий TG Payments лишився за `TELEGRAM_PAYMENTS_ENABLED` (default off), байт-в-байт недоторканий
- Окрема таблиця `wallet_topups` (а не `type`-колонка в `operator_payments`) + окремий роут `POST /webhook/wallet/{operator_id}`: розрізнення поповнення vs станційна сесія — на рівні роутингу, а не парсингом тіла. Станційний webhook, `test_operator_payments.py`, `reconcile_operators.py` — не зачеплені жодним байтом
- Нарахування балансу — через `update_user_balance` (єдина точка); `payment_id` вказує на рядок у `payments` (FK `kw_transactions.payment_id` саме туди), а не на `wallet_topups.id`
- **Блокер #1 (незалежне рев'ю Opus):** мʼютекс `set_wallet_topup_status` і кредит (`INSERT payments` + `update_user_balance`) спершу комітились ОКРЕМИМИ транзакціями — крах між ними лишав поповнення `success` без грошей безслідно (для `wallet_topups` немає звірки). Виправлено: обидва кроки в одній `conn.transaction()`, пуш винесено після коміту; регресійний тест доведено відкатом до старого коду; додано гард `conn.is_in_transaction()` у `_credit_wallet_topup_in_conn` (падає гучно поза транзакцією)
- Живий смоук-тест 2026-07-23: інвойс створюється (`pay.monobank.ua/...`), вебхук перепитує банк і на статусі `created` коректно НЕ нараховує, відповідає 200

**OCPP 3a — кістяк Central System** (міграція `0013_ocpp_station_fields`; нові `app/api/ocpp_ws.py`, `provision_ocpp_station.py`, `simulate_ocpp_cp.py`, `test_ocpp_central_system.py`; +18 central system, +13 ізоляція → 424 passed; PR #23, **задеплоєно 2026-07-24** — див. «Де ми»)

- WS-роут `/ocpp/{cp_id}` у наявному FastAPI (без нового event loop). `ocpp==2.1.0`, протокол 1.6J. Central System відповідає рівно на `BootNotification`/`Heartbeat`/`StatusNotification`; усе інше (Authorize/Start-Stop/MeterValues/RemoteStart-Stop) бібліотека сама відхиляє `CallError 'NotImplemented'` — це 3b/3c
- Реєстр зарядок перевикористано: `ocpp_charge_point_id` / `get_station_by_ocpp_charge_point_id` (з 4c) — нової сутності немає. Мультитенантність структурна (`ocpp_charge_point_id` глобально UNIQUE)
- OCPP Basic Auth (security profile 1): Fernet-ключ на станцію (`ocpp_auth_key_encrypted`, той самий `ENCRYPTION_KEY`), **свідомо поза `_STATION_FIELDS`** — dedicated-геттер (дзеркало monobank-токена) + тест, що звичайні SELECT-и його не віддають. Усі відмови на handshake — однаковий `close(1008)` без деталей (анти-оракул); пароль — `hmac.compare_digest`; уся перевірка ДО `websocket.accept()`
- Міграція `0013` (3 поля через `ALTER TABLE`) продубльовано в `init_operator_tables()` + **новий схема-дрифт тест саме на ALTER-колонки** (закриває клас бага 'refund')
- Реєстр `_active_charge_points` чиститься в `finally` умовно (`is charge_point`) — перепідключення станції не викидається старим з'єднанням (регресійний тест). У 3a реєстр лише для видимості; 3b використає його для команд
- Перевірено не лише `TestClient`: живий uvicorn + реальний WS-клієнт — справжній TCP/HTTP Upgrade, невірний пароль → справжня HTTP 403

**OCPP 3b — транзакції + метринг** (міграція `0014_ocpp_transactions`; +17 `test_ocpp_transactions.py`, +11 ізоляція → 452 passed; PR #24, **задеплоєно 2026-07-24** — див. «Де ми»)

- Нові @on-хендлери в наявному ChargePoint: Authorize (завжди Accepted — реальна авторизація за балансом у 3c), StartTransaction, StopTransaction, MeterValues (лише телеметрія, не джерело білінгу).
- StartTransaction ідемпотентно відкриває РІВНО ОДНУ 'charging'-сесію: дедуп по «активна сесія на станцію» (StartTransaction.req transactionId за спекою 1.6 НЕ несе), transactionId = id сесії, атомарно writable CTE; частковий унікальний індекс `WHERE ocpp_transaction_id IS NOT NULL` не чіпає наявний ручний money-флоу.
- StopTransaction: kWh = (meterStop − meterStart)/1000 (Decimal), ідемпотентний мʼютекс `status <> 'completed'`, захист від абсурдної дельти (сесія закривається з `kwh=NULL` + ERROR→`LOGS_CHAT_ID`, щоб не завісити 'charging'). Сесія резолвиться з БД по transactionId (переживає перепідключення станції).
- RemoteStart/StopTransaction (CS→станція) через наявний `_active_charge_points`, тенант-guard (чужу станцію не смикнути), двокроково; БЕЗ прив'язки до оплати. Сесії 3b без `payment_id`/user — `update_user_balance` не викликається (це 3c).
- Міграція `0014` (3 поля на `operator_sessions` + 2 часткові індекси) продубльовано в `init_operator_tables()`; генеричний схема-дрифт тест автоматично покрив (3→6 колонок).
- Живий прогін на реальному uvicorn (RemoteStart→Start→MeterValues→Stop) спіймав реальний баг (`id_tag_info` приходить plain dict, не датаклас) — виправлено.
- **Відоме обмеження (коментар у коді + беклог):** дедуп/індекс на рівні СТАНЦІЇ, не конектора — мультиконекторні станції поза обсягом 3b (потрібен `operator_sessions.connector_id`).

**OCPP 3c-i — резервація kWh-балансу (модель A)** (міграції `0015` enum `hold`/`release` + `0016` `charging_reservations`; нові `start_charging_session.py` [CLI-вхід, без UI бота], `reconcile_charging_reservations.py`, `test_charging_reservations.py`, `test_reconcile_charging_reservations.py`; PR #27, **задеплоєно 2026-07-24** разом із бот-адмінкомандами нижче)

- Ланцюг: `create_charging_reservation` (hold через `update_user_balance`, запобіжник `balance >= $1`, в ОДНІЙ `conn.transaction()`) → RemoteStart → StartTransaction активує резервацію за `id_tag` (звірка `operator_id`) → StopTransaction: `complete_ocpp_transaction_and_release` (списати факт, звільнити залишок, `finalized` — атомарно) → reconcile добирає застрягле (`pending` > 30 хв / `active` > 24 год)
- Модель B (грн, Monobank hold) — на **3c-ii**, гейт: живий смоук-тест

**Бот-адмінкоманди `/ocpp_start` `/ocpp_stop` — IN-PROCESS вхід у резервацію+зарядку** (без нової міграції; нові `app/services/ocpp_charging.py`, `app/handlers/ocpp_admin.py`; `start_charging_session.py` став тонкою CLI-обгорткою; +3 тестові файли; коміт `3978b10`, **ЗАДЕПЛОЄНО 2026-07-24**)

- **Знахідка (закрита):** `start_charging_session.py` (Промпт 3c-i) запускається окремим процесом (`docker compose exec`) — `_active_charge_points` живого uvicorn-процесу для нього завжди порожній, тож `RemoteStart` на реальному проді ЗАВЖДИ падав `ChargePointNotConnected`. Модель A була змержена (PR #27) технічно повною, але фактично незапускною на проді
- Рішення: спільне сервісне ядро (`app/services/ocpp_charging.py`, структурований результат замість `print`) винесено зі скрипта; нові бот-команди `/ocpp_start`/`/ocpp_stop` (`app/handlers/ocpp_admin.py`) працюють у ТОМУ САМОМУ event loop, що й OCPP WS-роут, тому реєстр реально доступний
- Гейт = `int(message.chat.id) == int(LOGS_CHAT_ID)` — та сама перевірка, що й `admin_activate_operator`. Початкова версія додатково вимагала `chat.type == "private"`; прибрано за рев'ю, коли підтвердилось, що `LOGS_CHAT_ID` на проді — супергрупа (`-100…`), тож ця вимога робила б команди недосяжними саме там, де адмін реально працює
- Реєстрація роутера перед `user_router` перевірена через справжній `aiogram.Dispatcher` (`test_ocpp_admin_router.py`), не лише коментарем
- **Жива валідація на проді (2026-07-24):** повний цикл із лог-супергрупи — `/ocpp_start` → hold 5.0 кВт·год → StartTransaction/MeterValues → `/ocpp_stop` → StopTransaction → факт 3.50 кВт·год + release 1.50 кВт·год → резервація `finalized`; баланс 10.00 → 6.50, арифметика точна

**Герметизація hold + документація крона звірки** (без нової міграції; +8 тестів у `test_ocpp_charging_service.py`/`test_ocpp_admin_handlers.py` → 534 passed, 4 skipped; гілка `feature/ocpp-hold-hardening`, коміт `7bd3bac`, **рев'ю Opus пройдено, 3 правки застосовано, змержено rebase'ом у `main`, ЗАДЕПЛОЄНО на прод 2026-07-25**)

- **Знахідка (закрита):** `start_charging_session()` (`app/services/ocpp_charging.py`) після успішного hold ловив ЛИШЕ `ChargePointNotConnected` між резервацією і RemoteStart — будь-який інший виняток (asyncpg-збій, таймаут, помилка ocpp-бібліотеки, `websockets.ConnectionClosed`, `asyncio.CancelledError` при рестарті контейнера) виходив із функції без release: резервація лишалась `'pending'`, hold висів, водій тихо втрачав кВт·год. До 2026-07-24 нешкідливо (шлях нічим було запустити на проді) — тепер живий (`/ocpp_start`)
- Пост-резерваційний блок обгорнуто в `try/except BaseException` (не `Exception` — `CancelledError` у Python 3.8+ від `BaseException`); release під `asyncio.shield()`, щоб компенсуюча корутина дожила до кінця навіть якщо саму задачу скасовано; внутрішній try/except навколо самого release логує збій, але НЕ маскує оригінальний виняток (`raise` без аргументу перевикидає саме його); завжди `raise` — неочікуваний виняток НЕ перетворюється на доменний статус `ChargingStartResult` (статуси — передбачені бізнес-гілки, баг не мусить осісти в них непоміченим)
- `app/handlers/ocpp_admin.py`: `cmd_ocpp_start`/`cmd_ocpp_stop` обгорнуто `except Exception` (НЕ `BaseException` — скасування задачі aiogram лишається скасуванням) навколо виклику сервісу — статичне повідомлення адміну замість тиші, текст винятку в чат не йде (той самий анти-оракул принцип, що в OCPP-хендшейку й Monobank-вебхуку), деталі лише в `logger.exception`
- `README.md`: команда `reconcile_charging_reservations.py` задокументована поруч із `reconcile_operators`/`reconcile_payments` (розділ «Звірка kWh-резервацій OCPP»), з явним попередженням про критичність `-T` для `docker compose exec` у cron (без нього — TTY-вимога, команда мовчки падає щогодини)
- Свідомо НЕ чіпали: `release_reservation_hold()`, `create_charging_reservation()`, `stop_charging_session()`, `on_stop_transaction`/`complete_ocpp_transaction_and_release()`, `reconcile_charging_reservations.py` (код), сам cron на сервері — усе за специфікацією бандла
- **Рев'ю Opus, 3 правки:** (1) повідомлення `/ocpp_start` про збій раніше стверджувало «резерв повернено» безумовно — неправда, якщо виняток стався в `create_charging_reservation()` (резервації ще нема) або якщо саме звільнення теж впало (крон ще не стоїть на сервері) — текст пом'якшено на «мав бути звільнений... ПЕРЕВІР баланс»; (2) коментар про `asyncio.shield()` стверджував, що без нього release взагалі не виконався б — емпірично на Python 3.11 неправда для одиночного `cancel()` (прапорець скасування знімається після першого кидка), shield захищає лише від ПОВТОРНОГО cancel — коментар і супутній `logger.error` переформульовано чесно; (3) наявний тест на `CancelledError` був синтетичним (`raise` з мока, жодної реальної задачі, `shield` не залучався) — доданий восьмий тест зі справжнім подвійним `task.cancel()`, що фактично провокує сценарій, який shield захищає (підтверджено red/green: без `asyncio.shield()` цей тест падає)
- Повний дифф + вивід тестів + розділ «Де тут рухаються гроші» → `review_prompt-ocpp-hold-hardening.md`
- **Деплой 2026-07-25:** змержено rebase'ом у `main` (`7bd3bac`), задеплоєно на прод; крон `reconcile_charging_reservations.py` налаштований (`30 * * * *`, лог окремо від `reconcile_operators`) — перший автоматичний прогін 04:30 UTC чистий (0/0)
- **Живий смоук ПІСЛЯ деплою — проведено 2026-07-25** на проді: станція `SMOKE-3C-2` (#4, оператор 1), водій `999000001`, симулятор усередині контейнера через `ws://127.0.0.1:8000/ocpp`. ДВА повні цикли поспіль: deposit +10.00 → hold −5.00 → спожито 3.500 → release +1.50, і ще раз те саме. Підсумок: баланс 3.00, обидві резервації `finalized`, обидві сесії `completed` з `kwh=3.500`, `operator_session_id` збігається з `ocpp_transaction_id` (6↔6, 7↔7) — два послідовні цикли не переплутались. Зупинка автоматична: `run_full_transaction_cycle()` у `simulate_ocpp_cp.py` скриптований (2×MeterValues +1500 Wh, потім StopTransaction +500 Wh = 3500 Wh), RemoteStop для симулятора не потрібен. Тестові дані прибрані. Бандл тепер живо валідований, не лише тестово покритий

**Самообслуговуваний онбординг оператора** (міграція `0017_operator_self_service` — `operators.monobank_token_verified_at`/`activated_at`/`is_public` + `ALTER COLUMN commission_pct SET DEFAULT 5`; нові `app/services/operator_activation.py`, `test_monobank_verify_token.py`, `test_operator_activation.py`; +55 тестів → 589 passed, 4 skipped; PR #32, коміт `d8d408b`, **рев'ю Opus пройдено, 3 правки застосовано, змержено rebase'ом у `main`, ЗАДЕПЛОЄНО на прод 2026-07-28, живий смоук пройдено того ж дня**)

- **Модель довіри перевернута:** pending → active автоматично, коли виконано ВСІ три об'єктивні критерії — профіль (є з реєстрації), токен Monobank підключено й **підтверджено реальним зверненням до банку** (`verify_merchant_token()`, `GET /api/merchant/details`, не просто збережений рядок), і хоч одна станція. `admin_activate_operator()` — НЕ видалено, лишається запасним ручним шляхом. Нова кнопка «🚫 Призупинити» (`admin_suspend_operator`) — головний запобіжник моделі
- **Критична межа безпеки, окрема від активації:** новий прапорець `operators.is_public` (default `FALSE`) — активація (навіть автоматична) НІКОЛИ сама не додає станції оператора в публічний водійський пошук (`list_public_stations_near`, тепер із фільтром `AND o.is_public = TRUE`); публічність — свідомо ручна дія (`set_operator_public()`, поки без бот-UI)
- Нова `app/services/operator_activation.py`: `OperatorChecklist`/`get_checklist()`/`try_auto_activate()` — атомарна ідемпотентна активація (`repo.activate_operator_if_pending()`, той самий ідіом, що `release_reservation_hold()`); новий токен скидає `monobank_token_verified_at` у `NULL` в тому самому запиті, що й сам токен (стара верифікація не чинна для нового значення)
- Кабінет: новий чек-лист-екран («📋 Прогрес активації», кнопка на кожен незакритий крок), «🔁 Перевірити токен ще раз» без повторного вводу токена; suspended-оператор бачить кабінет (перегляд не заблоковано), але не може мутувати нічого (нова станція/тариф/статус станції/токен) — раніше відкрите питання беклогу Промпту 4, вирішено явно
- **`commission_pct` DEFAULT 4→5 — застосовано 2026-07-28** (власник надав `SELECT id, name, status, commission_pct FROM operators ORDER BY id` з прода: рівно 1 рядок, id=1, `4.00`, лишається без змін). Три місця звірені: Python-параметр `create_operator()`, `migrations/0017` (`upgrade`), `init_operator_tables()` — плюс новий тест `test_commission_pct_default_matches_migration_bootstrap_and_python_param`, бо наявний schema-drift тест значень DEFAULT не перевіряє. Беклог-пункт «`commission_pct` не повинен мати DEFAULT узагалі» додано нижче
- **Рев'ю Opus, 3 правки:** (1) `activate_operator_if_pending()` писав `activated_at` безумовно, а `set_operator_status()` — через `COALESCE`; докстрінг стверджував спільну поведінку, код розходився — приведено до `COALESCE(activated_at, CURRENT_TIMESTAMP)` в обох; (2) `cabinet_verify_token()` не мав `_is_suspended` guard, як сусідні `cabinet_connect_token`/`cabinet_add_station_start` — дірки не було (`try_auto_activate` вимагає `status='pending'`), але призупинений оператор не мав змушувати систему ходити в банк; (3) вхід у майстер станції захищений, але FSM-стан у Redis переживає призупинення посеред майстра — guard додано ще й на `station_wizard_tariff_start()` (де `create_station()` реально пише в БД). Задокументовано (не змінено): `confirm_station_switched_on()` свідомо без guard — водій уже заплатив, блокувати підтвердження лишило б його без зарядки
- Повний дифф + вивід тестів + розділ «Де тут рухаються гроші» + свідомі девіації → `review_prompt-self-service-onboarding.md`
- **Живий смоук на проді — проведено 2026-07-28**, другий Telegram-акаунт, оператор id=2 «smoke onboarding test». Пройдено весь ланцюг: онбординг → pending з чек-листом (профіль ✅, токен ❌, станція ❌) + сповіщення в `LOGS_CHAT_ID` з кнопкою «Активувати» як запасним шляхом → невалідний токен (банк 401) → «❌ Банк не підтвердив токен», лишився `pending` (підтверджено: код розрізняє ВІДМОВУ банку від його НЕДОСТУПНОСТІ, а не трактує обидві як одне) → справжній токен (200) → «✅ Токен підтверджено банком», чек-лист перемальовується в тому самому повідомленні → додано станцію → **автоактивація спрацювала сама**, «🎉 Усі кроки пройдено», інформаційне сповіщення в `LOGS_CHAT_ID` з кнопкою, що змінилась на «Призупинити». Стан у БД після активації: `status=active`, `is_public=FALSE`, `activated_at` і `monobank_token_verified_at` заповнені
  - **Контрольний експеримент на межу публічності (найважливіше в смоуку):** перший підхід був зіпсований (тестова станція без координат — її відсутність у видачі нічого не доводила б, два фільтри дають один результат). Виправлено: координати за ~130 м від бойової станції, потім ТРИ пошуки поспіль зі зміною ЛИШЕ `is_public` (обидві станції весь час активні, обидва оператори активні, той самий радіус): `FALSE` → тестової нема, бойова є; `TRUE` → тестова з'явилась; `FALSE` → тестова зникла знову. Межа `is_public` доведена спостереженням, а не логікою коду
  - **Suspend перевірено:** оператор отримав «⛔ Ваш кабінет призупинено», перегляд станцій лишився доступним, клік по станції дає алерт «Кабінет призупинено» — read-only справді read-only, не «все закрито»
  - **Прибирання підтверджено:** одна транзакція, DELETE 1 станція + DELETE 1 оператор (сесій/резервацій/нарахувань у тестового не було), зашифрована копія бойового Monobank-токена видалена разом із рядком. У базі лишився оператор id=1 (`active`, `is_public=t`) і одна станція. **Бандл тепер живо валідований, не лише тестово покритий**

**Прибрано мертвий флоу «Банки»** (без міграції; видалено `app/api/payments.py` і `test_monobank_signature.py`; гілка `chore/remove-jar-flow`, **чекає рев'ю Opus, ще НЕ закомічено**)

- **Знахідка (закрита видаленням):** `app/api/payments.py` — тестовий вебхук Monobank «Банки» (`POST /webhook/monobank`, `/webhook/mono`), що нараховував кВт·год за Telegram-id із КОМЕНТАРЯ до платежу без перевірки існування користувача. Заміщений живим Monobank Acquiring (`app/api/wallet_webhook.py`) з 2026-07-23. Мертвий код, що рухає гроші, — найгірший вид мертвого коду (наживо не перевіряється, у рев'ю не потрапляє, ризик «увімкнути назад» через рік і отримати ту саму діру) — тому не пропатчений перевіркою `user_id`, а прибраний цілком
- Видалено: `app/api/payments.py` (сам файл), `test_monobank_signature.py` (5 тестів на `_verify_monobank_signature`, приватний хелпер файлу, що видаляється), імпорт + `app.include_router(payments_router)` у `app/main.py`, `MONOBANK_API_TOKEN` із `.env.example` і застаріла згадка `/webhook/monobank` у `docs/PROJECT_CONTEXT.md` (розділ «Безпека зовнішніх ендпоінтів»)
- **Чотири перевірки перед видаленням (жодна не наосліп):** (1) `MONOBANK_API_TOKEN` використовувався ЛИШЕ в `payments.py` — решта згадок (`operator_webhook.py`, `docs/evolt-white-label-bilinh-ta-p2p.md`, `scripts/gen_review.py`) — коментарі/історичні тексти, не код; (2) `PRICE_PER_KWH` живе в `app/database/connection.py`, `reconcile_payments.py` імпортує його НАПРЯМУ звідти — видалення `payments.py` константу не зачіпає; (3) пошук `/webhook/monobank`/`/webhook/mono`/`payments_router`/«Банку Monobank» — жодних живих залежностей поза `app/main.py` (виправлено) і документацією; (4) `mock_monobank.py` обслуговує ЛИШЕ acquiring-ендпоінти (`/api/merchant/invoice/create`, `/invoice/status`, `/merchant/details`) — жодного `StatementItem`/webhook «Банки», НЕ видалявся
- Свідомо НЕ чіпали: `app/api/wallet_webhook.py`, `app/api/operator_webhook.py` (живі acquiring-шляхи), `app/services/monobank_acquiring.py` (навіть попри данглінг-згадку `app/api/payments.py` у власному докстрінгу — файл прямо в списку «не чіпати»), історичні рядки в `kw_transactions`/`payments` (факти, не сміття), `docs/evolt-white-label-bilinh-ta-p2p.md` (стара разова інструкція з підготовки, не жива документація)
- `reconcile_payments.py`: одна дрібна правка коментаря (не логіки) — прибрано посилання на видалений `app/api/payments.py::monobank_webhook` у поясненні, чому пакетна ціна не рахується прямим діленням на `PRICE_PER_KWH`
- Тести: 589 → **584 passed, 4 skipped** (−5, рівно тести видаленого файлу; жоден інший тест не зачепився)
- Повний дифф + результати 4 перевірок + список свідомо залишеного → `review_prompt-remove-jar-flow.md`

**Живий смоук Monobank hold — гейт Моделі B (3c-ii) ПРОЙДЕНО** (без нової міграції й без нового production-коду; `mock_monobank.py` навчений формам у цьому ж бандлі — +3 нові ендпоінти (`paymentType: hold` у `invoice/create`, нові `invoice/finalize`/`invoice/cancel`), +12 тестів `test_mock_monobank.py` → 596 passed, 4 skipped; гілка `feat/mock-hold-forms-and-smoke-record`, смоук проведено 28–29.07.2026)

- Проведено на РЕАЛЬНИХ грошах: власна картка → власний мерчант (оператор №0), дві незалежні оплати по 20 грн, кожна проведена через увесь ланцюг hold
- **E0** — мерчант приймає `paymentType: "hold"` в `invoice/create` (перевірено створенням інвойсу без оплати — безкоштовно, нуль ризику)
- **E1/E2** (перша оплата) — утримано 20.00 грн → `status: hold` (гроші ЗАБЛОКОВАНІ, не списані) → `finalize` на 5.00 → `status: success`, `finalAmount: 500`, комісія перерахована 26→7 копійок. ПІДТВЕРДЖЕНО ФАКТИЧНО з обох боків: картка — «Часткове зняття банки “eVolt UA test” +15 ₴» того ж дня; мерчант — 29.07 о 05:37 зараховано 4.93 грн (5.00 − 0.07 комісії)
- **E3** (друга оплата, окремий hold) — 20.00 прожив 17 годин у `hold`, потім `cancel` БЕЗ фіналізації → банк відповів `{"status": "success"}`, інвойс перейшов у `reversed`, `finalAmount: 0`, з'явився `cancelList`. ПІДТВЕРДЖЕНО: картка — «Скасування. eVolt UA +20 ₴», комісії немає. **Це і був головний невідомий гейта:** cancel на НЕфіналізованому hold ПРАЦЮЄ, попри те що документація описує цей метод лише для успішної оплати
- Загальна звірка: по картці −20 −20 +15 +20 = −5; по мерчанту +4.93. Сходиться
- **Вхідні вимоги до реалізації 3c-ii** (не беклог — конкретні контрактні деталі, з якими має рахуватись майбутній код):
  а) читати `finalAmount`, а НЕ `amount` — `amount` це утримане, `finalAmount` списане; наявний `apply_bank_status` (`app/api/operator_webhook.py`) звіряє `amount` і для hold-інвойсів дав би хибний висновок
  б) на етапі `hold` поля `finalAmount` немає взагалі — стани розрізняються не лише за `status`
  в) `cancelList` — нова структура у відповіді, треба вміти читати
  г) `rrn`/`tranId` змінюються між hold і фіналізацією (`approvalCode` — ні): не будувати звірку на `rrn`
  д) `maskedPan` відрізняється між API та квитанцією (токен Google Pay): для звірки не використовувати
  е) комісія береться з ФАКТИЧНО СПИСАНОГО, не з утриманого — резервувати з запасом нічого не коштує
  ж) розрахунок із мерчантом приходить наступного ранку (~05:37), не миттєво: звірка має спиратися на СТАТУС інвойсу, а не на надходження. `reconcile_operators.py` уже так і працює — міняти не треба
  з) вебхуки приходять і на проміжних станах `created`/`processing` hold-інвойсу, не лише на фінальний `hold` — підтверджено живим смоуком 2026-07-30 (реальний production-код, не мок), код (`bank_status != "hold"` → тихий вихід) це переживає правильно
  и) банк шле вебхук-ШТОРМ: ≥3 дублікати підтвердження `hold` за ~250 мс на одну оплату (живий смоук 2026-07-30) — мʼютекс `mark_reservation_hold_confirmed` наживо відбив дублікати, рівно один `RemoteStart`; гонка, від якої проєктувався захист, виявилась реальною, не теоретичною
  і) одразу після `finalize` `invoice/status` короткочасно повертає ТРАНЗИТНИЙ `status: "processing"` з `finalAmount`, що дорівнює ПОВНІЙ утриманій сумі (не фактично списаній) — коректний `finalAmount` з'являється лише разом зі `status: "success"`. Полю `finalAmount` не можна вірити, поки статус не `success` (крок 4 звірки цього не порушує — читає `finalAmount` лише в гілці `success`)
  ї) відповідь самого `invoice/finalize` (`{"status": "success"}`) — ПІДТВЕРДЖЕНО ЖИВИМ СМОУКОМ 2026-07-30 (раніше було взято з документації банку без живого підтвердження)
  й) over-capture (finalize понад hold) банк ВІДХИЛЯЄ — HTTP 400, `{"errCode": "1001", "errText": "finalization amount exceeds hold amount"}`, hold лишається цілим; повторний `finalize` того самого інвойсу теж ВІДХИЛЯЄ — той самий `errCode "1001"`, `errText: "order on hold not found"` (розрізнити можна лише за текстом) — обидва тепер ФАКТ, підтверджено живим смоуком 2026-07-30 (раніше — консервативне припущення мока)
  к) комісія перерахувалась 26 коп. (hold 20 грн) → 7 коп. (списано 5 грн) — повторне живе підтвердження факту (е): комісія від фактично списаного, не утриманого
  л) повернення на картку після `cancel` — за ХВИЛИНИ (живий смоук 2026-07-30), швидше за «години» з попереднього смоуку 28-29.07; текст-попередження водієві («протягом кількох годин») лишити консервативним
- **Продуктове обмеження (не технічне):** водій бачить заблокованою ВСЮ зарезервовану суму певний час після зарядки. Уточнення за фактами смоуку: повернення на КАРТКУ приходить за ГОДИНИ того ж дня («Часткове зняття +15 ₴», «Скасування +20 ₴»), а не за дні; наступного ранку приходить лише зарахування на МЕРЧАНТ-рахунок. Отже в Моделі B ОБОВ'ЯЗКОВИЙ явний текст ДО утримання: скільки блокується, що спишеться лише фактично спожите, що залишок повернеться протягом кількох ГОДИН — без цього перший же водій напише «зняли 300 замість 60» і матиме рацію з того, що бачить. У моделі A на кВт·год цієї проблеми немає взагалі (баланс наш, release видно миттєво) — продуктова перевага моделі A, яка стала видимою лише після цього смоуку
- **Пастка інтерпретації (записано, щоб не переоткривати):** під час смоуку в стрічці Google Гаманця з'явився запис «ВІДХИЛЕНО: 20,00», і його спершу помилково прийняли за звільнення холду. Насправді це була НЕВДАЛА спроба оплати: на картці стояла заборона інтернет-платежів («Ви заборонили оплати в інтернеті»). Справжнє звільнення виглядає інакше і називається прямо — «Часткове зняття» / «Скасування», обидва зі знаком плюс. Google Гаманець — застосунок платіжного токена, а не виписка: для перевірки стану дивитись стрічку Monobank і виписку мерчанта
- Форми в `mock_monobank.py` — ЖИВІ, окрім відповіді самого `invoice/finalize` (тіло в смоуку не збережено, взято з документації банку, позначено в коді окремо коментарем джерела)
- Наступний крок: специфікація й реалізація 3c-ii з урахуванням вимог (а-ж) вище
- Повний дифф + вивід тестів + явне розділення живе/з документації → `review_prompt-mock-hold-forms.md`

**OCPP 3c-ii — Модель B, гривневий hold через Monobank** (міграція `0018_charging_reservations_uah`; нові `app/api/charging_hold_webhook.py`, `test_monobank_acquiring_hold.py`, `test_charging_hold_webhook.py`, `test_charging_reservations_live.py`; +101 тест → 697 passed, 7 skipped (+ 4 нові real-PG тести в `live-db-tests`); гілка `feature/model-b-uah-hold`, план — `docs/plan-3c-ii.md`, **рев'ю плану пройдено в три раунди ДО написання коду (конвенція CLAUDE.md); три раунди рев'ю КОДУ — два грошові блокери знайдено й закрито, третій раунд без зауважень; ЗМЕРЖЕНО ЄДИНИМ КОМІТОМ (`bf35ae1`→`main` `3b2d401` після rebase — міграція і код, що на ній працює, нероздільні) і ЗАДЕПЛОЄНО на прод 2026-07-29 ~20:50 UTC**)

- **Розширення `charging_reservations` (0016), не нова таблиця:** `payment_method` CHECK `'kwh'`→`'kwh'/'uah'`; `status` CHECK +`'awaiting_hold'` (інвойс створено, hold ще не підтверджений) +`'settling'` (атомарна локальна заявка на розрахунок ПЕРЕД дзвінком у банк); `reserved_kwh`/`user_id` — NOT NULL знято (uah-рядок не резервує kWh-баланс і не обов'язково має зареєстрованого водія); нові nullable `reserved_uah`/`invoice_id`/`final_amount_uah`/`driver_contact`; дискримінована CHECK-умова (рівно kwh-форма АБО рівно uah-форма); частковий унікальний індекс на `invoice_id`. CHECK-обмеження generic drift-тест не парсить (лише `ADD COLUMN`/`CREATE INDEX`) — новий явний тест звіряє їхній текст посимвольно між міграцією і `init_operator_tables()`
- **Fail-closed на рівні SQL (не «не забути if»):** `release_reservation_hold()`, `list_stale_pending_reservations()`, `list_stale_active_reservations()` фільтрують `AND payment_method = 'kwh'` у WHERE — без цього uah-рядок (`user_id=NULL`) дійшов би до `update_user_balance(user_id=NULL)` і впав би `NotNullViolation`
- **Джерело істини для грошей — БАНК, не наша БД:** `charging_reservations` для uah-рядків — лише локальний кеш останнього відомого стану банку. Три рівні захисту: (1) **головний** — атомарна локальна заявка через статус `'settling'` (`claim_reservation_for_settlement()`, мʼютекс `active`→`settling` ОДНИМ `UPDATE ... RETURNING`) — гарантує, що дзвінок у банк станеться щонайбільше з одного місця, БЕЗ покладання на непідтверджену ідемпотентність банку; (2) `reconcile_charging_reservations.py` — активний вирівнювач, СПЕРШУ перепитує банк, лише потім діє; (3) банк сам скасовує незакритий hold через 9 днів — природний останній рубіж
- **Тригер оплати — вебхук, не адмінський процес:** `POST /webhook/charging-hold/{operator_id}` (не вір тілу, той самий принцип 2a) підтверджує `hold`, мʼютексно переводить `awaiting_hold`→`pending`, шле `RemoteStart` — У ТОМУ САМОМУ uvicorn-процесі, що й `/ocpp` WS-роут (інваріант, знайдений рев'ю плану: `reconcile_charging_reservations.py` виконується ОКРЕМИМ процесом і `_active_charge_points` для нього завжди порожній — дослівне повторення знахідки «3c-i незапускний на проді», якби звірка сама намагалась стартувати зарядку). Герметизація (`try/except BaseException` + `asyncio.shield()`, той самий ідіом що `feature/ocpp-hold-hardening`): компенсація — НЕГАЙНИЙ `cancel_invoice()`, не `release_reservation_hold()` (fail-closed відмовить) і не очікування звірки
- **Розрахунок на `StopTransaction` (`on_stop_transaction`, гілка за `payment_method`):** `complete_ocpp_transaction_and_release_uah()` (`app/services/ocpp_charging.py`) — `complete_ocpp_transaction()` (мʼютекс сесії) → `claim_reservation_for_settlement()` → `compute_uah_settlement_amount()` (чиста функція: тариф_старт + тариф_кВт·год × факт, капована утриманим — Q4/варіант 1, перевитрату не стягуємо) → `finalize_invoice()`/`cancel_invoice()` (сума 0 — при `kwh=None`) → `record_uah_settlement()`. HTTP-виклик банку НЕ можна покласти в ту саму Postgres-транзакцію, що completion сесії — тому сесія ЗАВЖДИ завершується першою (незалежно від токена/станції), а збій дзвінка в банк лишає рядок `'settling'` (не перевикидається — settle не має ламати відповідь `StopTransaction`) для звірки
- **Звірка, чотири кроки** (контракт крона незмінний — exit code, `LOGS_CHAT_ID`, `--pending-minutes`/`--active-hours` + новий `--awaiting-hold-minutes`): (1) `awaiting_hold` застарілі — якщо банк підтвердив `hold`, а `RemoteStart` не відбувся — **лише алерт на ручний розбір, рядок НЕДОТОРКАНИЙ** (звірка НІКОЛИ сама не намагається стартувати зарядку — інваріант процесу вище); (2) `pending` застарілі (uah) — `cancel_invoice()`, якщо банк усе ще `hold`, синхронізація без повторного дзвінка, якщо банк уже сам дійшов до фіналу; (3) `active` застарілі (uah, **БЛОКЕР першого рев'ю коду — закрито**, див. нижче); (4) `settling` застарілі — поріг `SETTLING_STALE_SECONDS` (іменована константа, `assert` при імпорті модуля, що вона більша за `monobank_acquiring.DEFAULT_TIMEOUT` — інакше звірка могла б наздогнати ще живий у мережі виклик `finalize`/`cancel`)
- **Клієнт банку:** `create_invoice(..., payment_type="debit")` — новий параметр НЕ змінює payload для існуючих debit-викликів (`driver_qr.py`) ні на байт (поле `paymentType` додається в тіло лише коли не `"debit"`); нові `finalize_invoice()`/`cancel_invoice()`
- **Адмінкоманда `/ocpp_start_uah <operator_id> <station_id> <hold_amount_uah> [driver_contact]`** — той самий гейт, що `/ocpp_start`; на відміну від моделі A, RemoteStart тут НЕ синхронний (оплата водієм асинхронна) — команда лише створює hold-інвойс і повертає адміну `pageUrl` + канонічний текст-попередження для водія (водійського UI немає — свідома межа бандла)
- **Три раунди рев'ю плану ДО коду** (`docs/plan-3c-ii.md`): раунд 2 — 6 правок (fail-closed фільтри; статус `'settling'` замість покладання на банк; чесне розмежування «припущення нашого мока» vs «підтверджений факт живим смоуком» + новий пункт беклогу вище; герметизація вебхука з негайним `cancel_invoice()`; спільна чиста функція вартості); раунд 3 — 2 правки: (1) **переписано крок 1 звірки** — перша версія доручала звірці саму викликати `RemoteStart`, що дослівно повторило б «3c-i незапускний на проді» (звірка — окремий процес, `_active_charge_points` завжди порожній); виправлено на «лише алерт, рядок недоторканий»; (2) `SETTLING_STALE_SECONDS` — іменована константа з `assert`-обґрунтуванням відносно `DEFAULT_TIMEOUT`, а не довільне число в коментарі
- **Перше рев'ю КОДУ — 1 блокер, 2 не-блокери, усі закриті:**
  - **БЛОКЕР:** жоден із пʼяти запитів звірки не бачив uah-резервацію, застряглу в `'active'` (`payment_method`-фільтр закрив pending/active kwh-сестрам шлях, awaiting_hold/pending_uah/settling дивляться інші статуси) — планова правка «замінити крок на settling» мала бути «ДОДАТИ», а не «замінити»: реальний сценарій (StopTransaction ніколи не приходить — станція впала/кабель висмикнуто) лишав hold висіти в банку до 9-денного автоскасування БЕЗ жодного алерту, оператор не отримував нічого за фактично віддану енергію. Закрито новим кроком 3 (`list_stale_active_uah_reservations`, той самий поріг `--active-hours`, що для kWh): банк ще `hold` і привʼязана сесія вже `completed` (крах між `complete_ocpp_transaction()` і `claim_reservation_for_settlement()`) → рахуємо й фіналізуємо тією самою чистою функцією; сесії немає чи вона не `completed` (факт невідомий) → `cancel_invoice()` на ПОВНУ суму (той самий принцип, що kWh при `kwh=None`) + окремий `logger.error` (не лише звичайний запис у `released`) — застрягла `active`-резервація вартий уваги сигнал сам по собі
  - **Не-блокер 1:** `test_claim_reservation_for_settlement_returns_none_when_race_lost` доводив лише «мок повернув None → функція повернула None», не реальний захист від конкурентного claim на живому Postgres — докстрінг переписано чесно, і новий `test_charging_reservations_live.py` (розширює `live-db-tests` у CI поряд із `test_start_ocpp_transaction_live.py`) двома паралельними `asyncio.gather()`-викликами `claim_reservation_for_settlement()` на реальному PG підтверджує: рівно один переможець, резервація рівно один раз переходить у `'settling'` — перевірено проти живого локального Postgres, усі три нові тести пройшли
  - **Не-блокер 2:** капування перевитрати (`compute_uah_settlement_amount()`) писало лише `logger.error` — оператор про це ніколи не дізнається (не проходить через жоден крок звірки, не потрапляє в `LOGS_CHAT_ID`). Не виправлено кодом навмисно (протягування Telegram-контексту зіпсувало б чистоту функції) — задокументовано новим пунктом беклогу нижче з двома варіантами рішення для майбутнього бандла
- **Друге рев'ю КОДУ — 1 блокер, закрито:** сам новододаний крок 3 (`_reconcile_stale_active_uah`, щойно закрив перший блокер) і сусідній крок 2 (`_reconcile_stale_pending_uah`) дзвонили `finalize_invoice`/`cancel_invoice` БЕЗ попередньої атомарної локальної заявки — той самий клас гонки, від якого статус `'settling'` і вигадувався: станція, що повертається з офлайну й шле накопичений `StopTransaction` (штатна поведінка черги OCPP 1.6J), могла спричинити подвійний дзвінок у банк паралельно з живим шляхом `on_stop_transaction`. Закрито параметризацією `claim_reservation_for_settlement(expected_status: str = "active")` (SQL: `status = $3` замість захардкодженого `status = 'active'`) — обидва кроки звірки тепер беруть заявку (`expected_status="pending"` для кроку 2, дефолт `"active"` для кроку 3) перед будь-яким мутуючим дзвінком у банк і пропускають рядок (`stats.races += 1`, без дзвінка) при програші; два нові тести доводять «заявку програно → у банк НЕ дзвонили» для обох кроків
- **Третє рев'ю КОДУ — зауважень немає.** Бандл прийнято для мержу
- Повний дифф + вивід тестів → `review_prompt-ocpp-3c-ii-uah-hold.md`

**Живий смоук самої Моделі B на проді — гейт беклогу ЗАКРИТО** (без нового коду; проведено 2026-07-30 ~06:50–07:35 UTC, реальні гроші, власна картка → власний мерчант оператора 1, прод на `3b2d401`, коду під час смоуку не змінювали)

- **Наскрізно, не лише мок:** досяжність `/webhook/charging-hold/1` крізь nginx ззовні підтверджено окремо (запит із вигаданим `invoiceId` → HTTP 200, тіло 0 байт, у логах бота один WARNING «інвойс не знайдено серед резервацій» — шлях банк→nginx→uvicorn→код перевірено наскрізно перед грошима)
- Станція смоуку — **#6 `SMOKE-MODEL-B`** (`SMOKE-MB-1`, mode=ocpp, без координат — у публічний пошук не потрапляє), лишена в БД для майбутніх смоуків; OCPP-ключ ротовано після смоуку (перший ключ засвітився в чаті рев'ю)
- **`destination` водієві видно — гіпотеза підтверджена:** резервація #4 (hold 1 грн, не оплачувався) — на сторінці pay.monobank.ua текст «Зарядка SMOKE-MODEL-B: гарантія 1 грн, спишеться лише спожите» показаний прямо під сумою до вибору способу оплати. Що показує шторка Apple/Google Pay — не перевірялось
- **hold→finalize (щасливий шлях):** резервація #5, hold 50 грн, симулятор `SMOKE-MB-1` підключений. Оплата → вебхук `hold` → мʼютекс → RemoteStart → StartTransaction (сесія #8 прив'язана до #5 за 17 мс) → 3.500 кВт·год → StopTransaction → claim→compute→**finalize 35.00 з утриманих 50.00**. Від оплати до фіналізації — ~3 секунди. БД: #5 `finalized`, `final_amount_uah=35.00`; сесія #8 `completed`, `kwh=3.500`
- **hold→cancel (станція офлайн):** резервація #6, hold 20 грн, симулятор зупинений. Вебхук підтвердив hold → RemoteStart → `ChargePointNotConnected` → негайний `cancel_invoice()` (компенсація вебхука, не очікування звірки) → #6 `cancelled`, `final_amount_uah=0.00`; картка — «Скасування. eVolt UA +20 ₴» за хвилини
- **Експерименти на «сирому» hold-інвойсі** (без резервації, у жодну звірку не потрапляє, вебхук на нього коректно відповідав тихим 200 «не знайдено» — жива перевірка анти-оракула): over-capture (finalize 25 на hold 20) → HTTP 400 `{"errCode":"1001","errText":"finalization amount exceeds hold amount"}`, hold лишився цілим; легальний finalize 5.00 → `{"status":"success"}`; повторний finalize 5.00 на тому самому інвойсі → HTTP 400 `{"errCode":"1001","errText":"order on hold not found"}` — подвійного списання немає
- **Нові емпіричні факти про банк** — дописано в «вхідні вимоги (а-ж)» вище як (з)–(л): проміжні вебхуки на `created`/`processing`; вебхук-шторм ≥3 дублікатів `hold` за ~250 мс (мʼютекс `mark_reservation_hold_confirmed` відбив, рівно один RemoteStart/cancel — гонка виявилась реальною, не теоретичною); транзитний `processing`+`finalAmount`-повна-сума одразу після finalize; відповідь `invoice/finalize` підтверджена живцем; over-capture і повторний finalize — обидва відхиляються банком (`errCode "1001"`, різний `errText`); комісія від фактично списаного (повторне підтвердження); повернення на картку після cancel — за хвилини, не години
- **Закрито 4 пункти беклогу:** «живий смоук самої Моделі B на проді», «чи бачить водій `destination`» (так), «перевірити over-capture живим смоуком» (банк НЕ дозволяє, тепер факт — реалізація варіантом 1 єдино можлива, пункт «спростити compute_uah_settlement_amount(), якщо банк дозволить» закрито як неактуальний), «перевірити подвійний finalize живим смоуком» (банк відхиляє, факт; власний `'settling'`-claim лишається ГОЛОВНИМ захистом — принцип «не покладатись на банк» не змінюється, просто невідомої поведінки більше нема)
- **Дві нові знахідки в беклог** (читанням коду звірки під час смоуку, кандидати в окремий малий код-бандл зі своїм рев'ю — НЕ зроблено в цьому докс-бандлі): (1) крок 1 звірки (`_reconcile_stale_awaiting_hold`) трактує транзитний `processing` як «банк ніколи не тримав гроші» й позначає рядок `expired`, хоча факт (з) показав, що `processing` — реальний стан НА ШЛЯХУ до `hold`; якщо оплата довершиться в `hold` вже ПІСЛЯ того, як крон поставив `expired`, вебхук такий hold проігнорує (рядок не `awaiting_hold`) — гроші водія висітимуть до 9-денного автоскасування банку, а підсумок звірки ще й напише «звільнено» (чи дає банк довершити оплату після `validity` — не перевірено, гіпотеза; фікс дешевший за з'ясування: `processing` → пропустити рядок до наступного прогону, `expired` ставити лише для `created`/`expired`/`failure`); (2) else-гілка кроку 4 звірки (`_reconcile_stale_settling`, неочікуваний стан банку) пише лише `logger.error` без запису в `stats.alerts` — не потрапляє ні в Telegram-підсумок, ні в exit code, той самий клас невидимості, що вже задокументоване «капування перевитрати мовчки» нижче в беклозі
- Слід смоуку в БД/банку лишено свідомо (не прибирали): станція #6, резервації #4 `expired`/#5 `finalized` 50→35/#6 `cancelled` 20→0, сесія #8 `completed` 3.500 кВт·год — сліди реальних банківських операцій. «Сирий» інвойс `smoke-e5-overcapture` локального рядка не має ніде. Чистий кошт смоуку — ~пів гривні комісії банку (проведено 90 грн, повернеться все, крім 40 грн на мерчант мінус комісія)
- Оновлено коментарі (не логіку): `mock_monobank.py` (`finalize_invoice` — знято позначку «з документації», форми відмов приведено до живих `errCode`/`errText`), `app/services/monobank_acquiring.py::finalize_invoice()`, `app/services/ocpp_charging.py::compute_uah_settlement_amount()` — «консервативне ПРИПУЩЕННЯ» → «підтверджено живим смоуком 2026-07-30». Тести не зачіпались логічно; `test_mock_monobank.py` лишився зеленим (перевіряв лише `status_code == 400`, не текст)

**Карта: живий інфошар TomTom (31.07.2026)** (без нових міграцій; чотири бандли одного дня — `feature/tomtom-live-layer` (`821c182`+`a106698`) → `fix/merge-ocm-tomtom-layers` (`881cbf7`) → `feature/search-results-single-message` (`a0163a5`) → `feature/adaptive-radius-and-cards` (`5f5dfc0`); 697 → **781 passed, 7 skipped**; кожен бандл — окреме незалежне рев'ю Opus, усі чотири **ЗМЕРЖЕНО й ЗАДЕПЛОЄНО на прод 31.07.2026**)

- **Три шари водійського пошуку** (`handle_location`, `app/handlers/user.py`): операторські станції — шар ДІЇ (оплата через бот) → OCM — роз'єми, тарифи, широкий радіус 50 км → TomTom — потужність, конектор, LIVE-статус доступності конектора, радіус `TOMTOM_SEARCH_RADIUS_KM=15` км з адаптивним розширенням до `TOMTOM_FALLBACK_RADIUS_KM=30` км, коли перший запит повернув порожньо (і лише тоді — `None`, тобто недоступний шар, повтору не отримує, щоб не спалювати квоту)
- **Об'єднання джерел, не заміщення:** OCM і TomTom запитуються ЗАВЖДИ обидва (виправлена регресія, коли успішний TomTom вимикав запит до OCM). Та сама фізична станція, що прийшла з кількох джерел, схлопується в один запис `_dedupe_by_proximity` (поріг 100 м, `DEDUPE_DISTANCE_KM=0.1`): пріоритет оператор → більше метаданих (потужність/конектор) → TomTom при рівності метаданих
- **Квота видачі замість зрізу за відстанню** (`_select_by_quota`) — простий зріз топ-N за відстанню витісняв швидкі DC-станції сусідніми повільними AC: до `MAX_OPERATOR_SHOWN=2` операторських першими, до `MAX_DC_SHOWN=4` DC за відстанню, решту до `MAX_STATIONS_TOTAL=6` добирає AC (і навпаки, якщо DC мало)
- **Рендер — окремі картки на станцію** (прапорець `SEARCH_SINGLE_MESSAGE`, дефолт `false` — рішення власника 31.07.2026 за результатом живого смоуку, див. п. 9 «Беклогу»). Одноповідомленнєвий рендер (одне HTML-повідомлення на всю видачу, навігаційні кнопки в одній клавіатурі) лишається в коді за прапорцем `=true`, не видалений

**Юридична рамка TomTom** (важливо для майбутніх рішень — не загубити):

- **T&C п. 11.4** забороняє кешування даних для обслуговування кількох користувачів — тому архітектура свідомо «живий запит під конкретного водія, без БД і без кешу» (`app/services/tomtom_service.py`, докстрінг модуля прямо посилається на цей пункт)
- **T&C п. 20.2.3 виявився ВУЖЧИМ, ніж вважалось спочатку:** він забороняє змішувати дані так, щоб на контент TomTom поширилась open-source ліцензія (напр. якби TomTom-дані й OCM-дані зливались в один запис із єдиною ліцензією-парасолькою) — окремі шари з окремою атрибуцією кожного джерела (`© TomTom`, `© Open Charge Map`) цього не порушують, тому архітектура «шари, не злиття даних» лишається юридично безпечною
- Безкоштовний рівень TomTom — 2500 запитів/добу; реальна арифметика (див. коментар біля `TOMTOM_DAILY_BUDGET`) — до 8 звернень на один пошук водія (2 nearbySearch за адаптивного радіуса + до 6 get_availability) ≈ **250 пошуків водіїв на добу**
- **Лист у підтримку TomTom про коротку буферизацію надіслано 31.07.2026, відповіді ще немає** — якщо дозволять, зняло б частину обмежень (менше живих запитів на той самий результат); до відповіді архітектура лишається «живий запит без кешу»

**Виміряне покриття України** (розвідка 30–31.07.2026, скрипти в `scripts/research/` — НЕ комітились у git, разові):

- OCM: **572** станцій. OSM: **907**. Перетин OCM∩OSM: **138**. Об'єднання OCM∪OSM: **1341**
- Для порівняння — PlugShare заявляє **~9864** точок в Україні. **Застереження, щоб число не прочиталось хибно:** PlugShare включає домашні зарядки (не публічні), а ринкові оцінки часто рахують КОНЕКТОРИ, не станції (одна станція = кілька конекторів) — пряме порівняння з OCM/OSM (станції) завищує розрив
- Львів (де живцем перевірявся ключ TomTom): TomTom бачить ~100 станцій у радіусі 15 км, 51 із них із real-time статусом, 20 DC-конекторів — **збігається з незалежним ручним підрахунком власника** (96 станцій, 21 швидка) — довіра до живих цифр TomTom підтверджена не лише кодом, а й людським підрахунком
- Вимір покриття через Eco-Movement (окреме джерело, AWS Data Exchange) — ще НЕ зроблено, див. новий пункт «Беклогу» нижче

**Безпека — ключ TomTom витікав у логи** (виявлено живим смоуком 31.07.2026, закрито тим самим днем, `fix/merge-ocm-tomtom-layers`):

- `httpx` на рівні `INFO` друкує повний URL кожного запиту, включно з query-параметрами; TomTom приймає ключ саме в query (`?key=...`) — на відміну від OCM/Monobank/OCPI, які передають секрети в заголовках (`X-API-Key`/`X-Token`/`Authorization`, перевірено — їх ця діра не стосується). Ключ лежав відкритим текстом у логах контейнера при КОЖНОМУ пошуку станцій
- Закрито `logging.getLogger("httpx").setLevel(logging.WARNING)` в `app/main.py` — глушить проблему для всіх поточних і майбутніх `httpx`-викликів одним рядком, без точкових правок по кожному сервісу
- **Ключ TomTom ротовано 31.07.2026** (новий ключ у `.env` на проді)

**Правило, засвоєне сьогодні** (записано як інваріант процесу, не як історичний факт — застосовувати для будь-якої майбутньої інтеграції зовнішнього API):

> Форму відповіді зовнішнього API звіряти ЖИВИМ запитом ДО написання мока/парсера, не після.

Сьогодні це врятувало двічі в один день (`feature/tomtom-live-layer`, рев'ю Opus живими запитами 31.07.2026): `chargingPark` лежить на ВЕРХНЬОМУ рівні `result` (не всередині `poi`, як передбачав перший прохід); параметр статусу доступності зветься `chargingAvailability` (не `chargingAvailabilityId`, як передбачав перший прохід). Обидва були б ТИХИМИ багами — фіча мовчки не працювала б на проді, тести лишались би зеленими (мок повторював ті самі хибні припущення, що й код). Той самий клас проблеми, що PR #25 (writable-CTE) і «мок vs реальність» (`mock_monobank.py`).

**Бар'єр проти секретів у комітах (01.08.2026)** (`scripts/check_secrets.py`, job `secret-scan` у CI, git-хук `scripts/pre-commit`; +`test_check_secrets.py`; гілка `chore/secret-scan-barrier`, змержено rebase'ом у `main` як `aa5e34f`, попередній стан `5566a42`; **редеплою не потребує** — у контейнер не входить, працює лише на CI/локальному git)

Причина бандла — два витоки ключів 31.07.2026 (TomTom-ключ у логах `httpx`; Anthropic-ключ в `auth_check.py`, див. нижче), жоден не спіймано процесом.

*Реалізація — три раунди незалежного рев'ю Opus, ДВА блокери знайдено й закрито; обидва такі, що бар'єр ВИГЛЯДАВ робочим і не працював:*

- **(а) Сканер не проходив власний бар'єр:** exit 1 на власному staged-диффі, 48 хибних спрацювань на 80 production-файлах, справжніх секретів 0. Причина: правило «чутливе присвоєння» дивилось на ІМ'Я змінної (`KEY`/`TOKEN`/`SECRET`/`PASSWORD`) і ДОВЖИНУ значення, але не на те, чи значення взагалі літерал — тож ловило вирази на кшталт `await repo.get_operator_token(...)`, `os.getenv(...)`, `location_key_builder` без лапок. Документований шлях відступу (розширити allow-list) для цього класу не працював у принципі: довелося б виключити `app/api/*.py`. Фікс: значення має бути ЛІТЕРАЛОМ секретної форми, не довільним python-виразом. Символ `_` у наборі дозволених символів ЛИШЕНО свідомо — заміряно на 10000 згенерованих ключів Fernet: 47.8% містять `_`, прибрати його означало б зробити майже кожен другий `ENCRYPTION_KEY` невидимим. Окрема гілка для суцільного hex з порогом 32 (ловить lowercase-hex ключі на кшталт `OCM_KEY`; поріг саме 32, щоб не чіпати Alembic revision id — вони теж hex, але 12 символів)
- **(б) CI-job був no-op на прямому пуші в `main`** (`origin/main` після фетчу дорівнює `HEAD` → порожній диф → зелений job, що не переглянув жодного рядка) **і fail-OPEN на недосяжному base ref** (`git` exit 128 → сканер exit 0), попри коментар у `ci.yml`, який обіцяв fail-closed. Фікс: база з `github.event.before` на push, `origin/main` на pull_request, фолбек на нульовий SHA; перевірка досяжності через `git cat-file -e` з видимим `::warning::` при відкоті; ненульовий код повернення git тепер дає скрипту exit 1, а не тихий «чисто»

*Не-блокери, вкладені тим самим бандлом:* позиційні патерни Telegram bot-токена, секрету в query-рядку URL і рядка підключення з паролем усередині (жодну з цих форм правило «ІМ'Я=значення» не бачило); YAML-дефіс у регулярці присвоєння (форма списку `docker-compose.yml`); дедуплікація знахідок за (файл, рядок) — один ключ раніше давав 3 окремі знахідки; виняток для URL-схеми (константа виду `MONOBANK_TOKEN_URL` з `https://...`-значенням хибно ловилась); `.env.example` і `docs/*.md` прибрані з allow-list ПІСЛЯ емпіричної перевірки 0 спрацювань на їхньому реальному вмісті; `.gitignore` тепер `.env*` з винятком `!.env.example`; `is_env_file` розпізнає `.env*` окрім `.env.example`; явний `encoding="utf-8"` у `subprocess` (без нього скрипт падав `UnicodeDecodeError` на першому українському коментарі під Windows).

Ключовий новий тест — `test_no_false_positives_on_real_production_python_files`: проганяє сканер по всіх `git ls-files '*.py'` поза allow-list і вимагає нуль знахідок. Його відсутність і була причиною, чому 48 хибних спрацювань лишались невидимими при повністю зелених тестах — усі попередні тести ганяли сканер по СИНТЕТИЧНИХ рядках. Той самий клас проблеми, що PR #25 (writable-CTE) і мок TomTom.

Тести: `test_check_secrets.py` — **94 passed**; уся сюїта — **875 passed, 7 skipped**.

*Живий смоук на справжньому GitHub Actions, 01.08.2026* (зелені тести не доводять, що job спрацює на проді CI — job зелений і тоді, коли не бачить жодного рядка, тому окрема перевірка ЧЕРВОНИМ):

- Одноразова гілка `test/secret-scan-smoke`, один коміт з фейковим ключем. Job `secret-scan` — ЧЕРВОНИЙ за 5 с, у лозі видно справжній `github.event.before` (не фолбек на `origin/main`)
- Job `test` упав НЕЗАЛЕЖНО від `secret-scan` — обов'язковий тест по реальному репозиторію знайшов той самий файл. Два рубежі спіймали витік окремо один від одного. `live-db-tests` лишився зеленим
- У публічному Actions-лозі надруковано лише усічений фрагмент (`_truncate_snippet`), не ключ цілком — на публічному репозиторії це не косметика: інакше кожне майбутнє спрацювання на справжньому ключі публікувало б його у відкритий білд-лог
- Гілку видалено локально й на GitHub, слідів не лишено
- Пуш у `main` (`aa5e34f`): job diff-ив реальний попередній коміт (`5566a42`), job зелений — прямий пуш у `main` тепер реально сканується, не проходить повз
- Локальний хук встановлено і перевірено: коміт із фейковим ключем ЗУПИНЕНО, `HEAD` не зрушив

*GitHub Secret scanning — перевірка репозиторію, 01.08.2026* (Secret scanning і Push protection увімкнено на GitHub):

- Виявлено рівно 2 алерти, обидва з закоміченого `.env` і обидва відкриті ще 09.07.2026 (публічні репозиторії скануються завжди — алерти лежали непрочитаними 3 тижні): GCP API Key (`GEMINI_API_KEY`), Telegram Bot Token (`BOT_TOKEN`). Обидва ключі вже перевипущені, старі значення видалені — алерти закрито як Revoked
- Anthropic-ключа в списку GitHub НЕМАЄ, попри той самий публічний репозиторій. Гіпотеза (НЕ перевірено): ключ був приклеєний до `EOF` без розриву рядка (невдалий heredoc у PowerShell), а партнерські патерни GitHub вимагають межу слова перед префіксом `sk-ant-`. Наш `scripts/check_secrets.py` цю форму ловить (перевірено — `_scan_prefixes` шукає підрядок, не вимагає межі слова)
- Витік Anthropic-ключа в `auth_check.py` тривав 2026-07-16 → 2026-07-31 (коміт `5566a42`, до нього — коміт `9151fd1`, який іронічно називався «прибрано секрети та додано .gitignore»). Ключ видалено з файлу 31.07. Git-історію свідомо НЕ переписували: ключ відкликано в консолі Anthropic 31.07.2026 — саме відкликання, а не лише заміна значення у файлі (створення нового ключа старий не вимикає); відкликання закриває реальний ризик, а `filter-repo`/BFG зламав би всі SHA, на які посилається цей документ і `review_prompt-*.md`

**Операторський UX перед першим стороннім оператором (02-03.08.2026)** (гілка `fix/operator-ux-before-first-tenant`, коміт `975e0ea`, **ЗАДЕПЛОЄНО 2026-08-02 ~21:13 UTC**; без міграцій)

Мета бандла — прибрати те, що стає реальним рівно тоді, коли системою скористається не власник.

- **Токен Monobank більше не зберігається до перевірки банком.** Було: `encrypt` + запис одразу при отриманні тексту, перевірка потім — помилка при копіюванні у АКТИВНОГО оператора знищувала робочий токен, скидала `verified_at` і зупиняла оплати до повторного правильного вводу. Стало: новий `_verify_and_save_new_token()` — банк питають ПЕРШИМ, у БД не пишеться нічого, поки не підтверджено. Наявний `_verify_token_and_respond()` лишився для шляху «🔁 Перевірити токен ще раз» (перевіряє ВЖЕ збережений). Розрізнення «банк відмовив» / «банк недоступний» збережено — різні тексти. Видалення повідомлення з токеном лишилось на тому самому РАННЬОМУ місці (до дзвінка в банк) — інакше при повільному банку токен висів би в чаті
- **ІНВАРІАНТ, який ця зміна встановлює:** збережений токен = токен, який банк колись підтвердив. Виняток — легасі-рядки до цієї зміни (оператор id=1, у нього `verified_at` порожній)
- **Порядок save+mark — свідоме рішення, ЧОМУ зафіксовано в коді.** Лишились ДВА репо-виклики (`set_operator_monobank_token` → `mark_operator_token_verified`), а не один UPDATE. Причина: перший скидає `verified_at` у NULL як другий рубіж (щоб онбординг не можна було обійти підміною токена після автоактивації), другий викликається лише після живої перевірки. Вікно між ними безпечне ОДНОБІЧНО: збій лишає стан «токен є, не підтверджений» — система вдає гіршу, ніж є, самолікується кнопкою, грошей не чіпає. Стану «підтверджено, хоч банк не питали» за побудовою не існує. Один UPDATE прибрав би вікно ціною функції, здатної підтвердити токен без банку одним викликом. **НЕ зливати ці два виклики в майбутніх бандлах**
- Текст статусу pending: «⏳ Заявку на розгляді» → вказівка на чек-лист. Ніхто нічого не розглядає — старе формулювання змушувало оператора чекати замість закривати кроки
- `get_cabinet_menu(..., is_suspended)` ховає МУТУЮЧІ кнопки для призупиненого; «Мої станції» і «Виручка» лишаються (перегляд свідомо відкритий, рішення бандла self-service-onboarding)
- Сповіщення адміну більше не дублює id за відсутнього нікнейма
- Гард `_is_suspended` доданий у `save_monobank_token()` — знахідка рев'ю: вхід (`cabinet_connect_token`) був захищений, але FSM-стан у Redis переживає призупинення посеред вводу, а пише в БД саме цей хендлер. Дослівний повтор прецеденту `station_wizard_tariff_start` з попереднього бандла

*Рев'ю:* план узгоджено ДО коду (конвенція `CLAUDE.md`, правило 2 — шлях токена). Одне зауваження в коді (гард вище), закрито. Тест на гард сконструйовано адверсарно: шпигун `verify_merchant_token` повертає `True`, тож без гарда токен записався б і тест упав би — red/green видно з конструкції

*ЖИВИЙ СМОУК на проді 2026-08-03 ~00:16 EEST:* знімок рядка `operators` id=1 (стан токена, `verified_at`) ЗРОБЛЕНО ДО і ПІСЛЯ надсилання завідомо неправильного токена — значення збіглися повністю. У чаті: повідомлення з токеном видалено, «Перевіряю токен…» без слова «збережено», вердикт «❌ Банк не підтвердив… попередній лишився без змін». Доказ — незмінений рядок у БД, не текст у чаті

**Закриття публічного Swagger/OpenAPI (03.08.2026)** (гілка `fix/disable-public-api-docs`, коміт `732d8d8`, **ЗАДЕПЛОЄНО 03.08.2026**; без міграцій)

Живий запит 03.08.2026 підтвердив: `/openapi.json` на проді віддавав повну специфікацію API, включно з трьома грошовими вебхуками й OCPI-шляхами з `{user_id}` — деталі й закриття беклог-пункту → «Беклог», розділ 3. Новий прапорець `ENABLE_API_DOCS` (`app/main.py`), дефолт ВИМКНЕНО (той самий ідіом, що `TELEGRAM_PAYMENTS_ENABLED`) — вимикає `docs_url`/`redoc_url`/`openapi_url` разом. Межі бандла: лише `app/main.py` і `.env.example`, роути/авторизацію/вебхуки/nginx не чіпали.

**Методична знахідка при написанні тесту (зафіксувати, щоб не повторити):** `docs_url`/`openapi_url` читаються ОДИН РАЗ при конструюванні `FastAPI(...)`, тож перевірка "дефолт вимкнено" вимагала окремого процесу на кожен сценарій (`importlib.reload(app.main)` у тому самому процесі падає — переприв'язує вже приєднані aiogram-роутери до спільного `dp`, `RuntimeError: Router is already attached`, і псує стан для решти тестів сесії). Перша версія тесту "за замовчуванням" робила `env.pop("ENABLE_API_DOCS")` для дочірнього процесу — **негерметично**: `app/core/loader.py` викликає `load_dotenv()` (default `override=False`), тож відсутнє в оточенні ім'я підхопилось би з РЕАЛЬНОГО `.env` розробника, а `.env.example` якраз радить `ENABLE_API_DOCS=1` для локального дебагу — тест почав би падати без жодної зміни коду. Замінено на `env["ENABLE_API_DOCS"] = ""` (порожній сентинел, `load_dotenv()` не перезаписує вже присутнє в оточенні ім'я). **Дірку доведено ЕМПІРИЧНО, не лише логікою:** у справжній `.env` дописано `ENABLE_API_DOCS=1`, тест перезапущено — з `env.pop()` він упав би, з сентинелом пройшов; `.env` відновлено одразу після.

Тести: `test_api_docs.py` — 2 нові, кожен окремим процесом (default: `/openapi.json`/`/docs`/`/redoc` → 404, `/health` живий; `ENABLE_API_DOCS=1`: усі три → 200). Уся сюїта — 886 passed, 7 skipped.

## Рев'ю: статус

Рев'ю 1, 2a і 2b пройдені, усі правки застосовані. **Відкритих зауважень немає.**

Промпт 4 (кабінет оператора) — перший прохід рев'ю пройдено, три правки застосовані:
1. `test_operator_b_stations_list_excludes_operator_a` перевіряв текст повідомлення, а назви станцій живуть у кнопках `reply_markup` — тест проходив завжди незалежно від реального результату. Виправлено: перевіряються тексти кнопок.
2. Підтверджено, що `cmd_start` (`app/handlers/user.py`) чистить FSM-стан і зареєстрований з `StateFilter("*")`, тож `/start` посеред майстра станції справді виводить з нього — додано регресійний тест на це замість покладатись лише на коментар. Невикористаний `ALL_STATES` прибрано з `app/states/operator_states.py`.
3. `save_monobank_token` у гілці «не приватний чат» тепер теж намагається `bot.delete_message` — токен, надісланий у групу попри перевірку на кроці входу, не мав лишатись видимим.

Не блокує PR, у беклог (розділ «Беклог», п. 6 нижче): часовий пояс «сьогодні» у виручці, мовчазне обрізання CSV на `limit=1000`, доступ `suspended`-оператора до кабінету, онбординг у групових чатах.

**Промпт 4 змержено в `main` (PR #13).** Міні-фіча «активація кнопкою» — **змержено в `main` (PR #14), рев'ю пройдено**. Локальну гілку `feature/operator-activation` видалено.

**Промпт 4c змержено в `main` (PR #15)** і докочений на прод 2026-07-21 увечері — перевірено наживо, об'єднаний пошук працює.

**Промпт 5 змержено в `main` (PR #16)**, рев'ю не проводилось окремо. **Код на проді** (перезбірки 2026-07-23). Звірку `reconcile_operators.py` перевірено наживо: два ручні прогони (перший — 2 виправлено автоматично / 0 на ручний розбір / exit 0; другий — 0/0/0, exit 0 → ідемпотентність підтверджена), пуш підсумку в `LOGS_CHAT_ID` працює. **Cron щогодини додано** (`0 * * * *`, вивід у `/var/log/ev-charge-bot-reconcile.log`), поруч зі щоденним бекапом Postgres о 3:00.

**Мок vs реальність** (`fix/mock-vs-reality`) — міні-задача, PR можна відкривати одразу (зміна мала, лише форма відповіді мока). Рев'ю не подавалось.

**wallet-realmono змержено в `main` (PR #22)** — **повне незалежне рев'ю Opus пройдено**. Знайдено 1 грошовий блокер (неатомарний кредит без страхувальної звірки для `wallet_topups`) → виправлено атомарною транзакцією + гард `is_in_transaction()` + регресійний тест (доведений відкатом). Решта зауважень — дрібниці в беклог. 392 passed, CI зелений. Задеплоєно й перевірено наживо 2026-07-23 (інвойс + вебхук).

**OCPP 3a змержено в `main` (PR #23)** — **повне незалежне рев'ю Opus пройдено**. Блокерів немає; знайдено 1 латентний пункт (реєстр `_active_charge_points` — безумовний `pop` у `finally` міг викинути перепідключене з'єднання, наслідки в 3b) → закрито ідентичність-гардом + регресійним тестом (доведений відкатом). Верифіковано напряму з коду (auth, тенант-скоуп нових функцій, міграція==бутстрап). 424 passed, CI зелений. **Задеплоєно 2026-07-24** (nginx `/ocpp` + жива OCPP-станція — див. «Де ми»).

**OCPP 3b змержено в `main` (PR #24)** — **повне незалежне рев'ю Opus пройдено**. Блокерів немає. Верифіковано напряму з коду: ідемпотентність Start/Stop (ретраї), атомарний writable CTE + часткові унікальні індекси (гонка ловиться UniqueViolation), kWh=meterStop−meterStart з delta-guard, транзакція→сесія з БД, тенант-скоуп, міграція 0014==бутстрап (колонки+індекси). Дві свідомі девіації правильні (`'completed'` уже в CHECK; Start-дедуп по активній сесії — виправив помилку в моєму промті). Знахідка (не блокер): модель per-station, не per-connector → задокументовано коментарем у коді + беклог. 452 passed, CI зелений. **Задеплоєно 2026-07-24.**

**Хотфікс `start_ocpp_transaction` (PR #25)** — рев'ю Opus. Пре-існуючий 3b-баг: writable-CTE `UPDATE` не бачив щойно вставлений CTE-рядок → `ocpp_transaction_id` NULL → StartTransaction Invalid; ловиться лише реальним PG (моки ховали). Фікс: 2 кроки в `conn.transaction()`; real-PG регресійний тест (red→green доведено); скан репозиторію — єдиний екземпляр цього класу бага.

**OCPP 3c-i (PR #27)** — повне рев'ю Opus, блокерів немає. 4 гарантії витримані: hold через єдину точку `update_user_balance` з `balance >= $1`; списання=факт (hold−release); ідемпотентність (мʼютекси + WHERE-status + атомарний RETURNING); reconcile ловить застрягле. Тенант: композитний FK + звірка `operator_id` в активації (`id_tag` — секрет). 491 passed.

**Бот-адмінкоманди `/ocpp_start`/`/ocpp_stop`** — рев'ю Opus, одна правка застосована (адмін-гейт: прибрано хибну вимогу `chat.type == "private"`, підтверджено, що `LOGS_CHAT_ID` на проді — супергрупа). Реєстрація роутера й release-on-failure гарантія перевірені окремими тестами (справжній `aiogram.Dispatcher` + нетто-баланс 0). 526 passed. **Змержено й задеплоєно 2026-07-24**, повний живий цикл резерв→факт→release перевірено через реальні бот-команди з лог-супергрупи.

**OCPP 3c-ii — Модель B (гривневий hold через Monobank)** — три раунди рев'ю плану ДО коду (конвенція `CLAUDE.md`) + **три раунди рев'ю коду**. Два грошові блокери знайдено й закрито (застрягла `active`-резервація поза видимістю звірки; звірка дзвонила в банк без атомарної локальної заявки — див. «Зроблено»), третій раунд — без зауважень. 697 passed, 7 skipped. **Один коміт на весь бандл** (`bf35ae1` → `main` `3b2d401` після rebase) — міграція `0018` і код, що працює з новою схемою, нероздільні. **Змержено й ЗАДЕПЛОЄНО на прод 2026-07-29 ~20:50 UTC**: Alembic 0017→0018 підтверджено логом деплою, схема звірена наживо (`\d charging_reservations`), крон `reconcile_charging_reservations.py` — 5 прогонів 00:30–04:30 UTC, усі 0/0. Живий смоук самого hold→finalize/cancel через Модель B (не лише мок) — **ПРОЙДЕНО 2026-07-30**, наскрізно на проді, реальні гроші, див. «Зроблено».

**Бар'єр проти секретів у комітах (`chore/secret-scan-barrier`)** — **три раунди незалежного рев'ю Opus**. Два блокери, кожен закрито в наступному раунді після вимірювання, не на слово: (а) власний сканер не проходив свій же бар'єр (48 хибних спрацювань на 80 production-файлах, справжніх секретів 0 — правило «чутливе присвоєння» ловило python-вирази замість літералів), фікс — вимога секрето-подібної ФОРМИ значення; (б) CI-job — no-op на прямому пуші в `main` і fail-OPEN на недосяжному base ref, попри коментар, що обіцяв fail-closed (другий раз поспіль коментар стверджував неперевірену гарантію), фікс — `GitDiffError` на рівні коду + перевірка досяжності в `ci.yml`. Кожен блокер відтворено ЗАПУСКОМ перед фіксом і після. Третій раунд — без зауважень до коду; **живий смоук на справжньому GitHub Actions ПРОЙДЕНО 01.08.2026** (одноразова гілка з фейковим ключем, job `secret-scan` червоний за 5с, `test` упав незалежно, гілку видалено — див. «Зроблено»). `test_check_secrets.py` — 94 passed, уся сюїта — 875 passed, 7 skipped. Змержено rebase'ом у `main` (`aa5e34f`), редеплою не потребує.

**Операторський UX перед першим стороннім оператором (`fix/operator-ux-before-first-tenant`)** — план узгоджено ДО коду (шлях токена Monobank, конвенція `CLAUDE.md`). Один раунд рев'ю коду, одне зауваження: гард `_is_suspended` був відсутній у `save_monobank_token()`, хоча саме вона тепер пише в БД (вхід у флоу гардований, FSM-стан посеред вводу — ні) — закрито. Живий смоук на проді пройдено 2026-08-03 (звірка незмінного рядка в БД до/після спроби невалідного токена, не лише тексту в чаті). 884 passed, 7 skipped. Змержено rebase'ом у `main` (`975e0ea`), **ЗАДЕПЛОЄНО 2026-08-02 ~21:13 UTC**.

Свідомо відкладене (задокументоване коментарями в коді, не баги):

| Що | Де | Коли закривати |
|---|---|---|
| ~~Кроки платіж→success, сесія→paid, дохід не в одній транзакції~~ | `app/api/operator_webhook.py` | **закрито Промптом 5** — звірка (`reconcile_operators.py`) добирає перерваний ланцюг тим самим ідемпотентним шляхом |
| ~~Вікно «інвойс у банку створено, `operator_payments` не встиг»~~ | `app/api/driver_qr.py` | **закрито Промптом 5** — сценарій 4 звірки алертить на pending-сесії без `payment_id` старші за годину (автоматично не виправляється, invoice_id невідомий — потрібна ручна звірка з випискою банку) |
| Процедури ротації `ENCRYPTION_KEY` немає | `app/core/crypto.py` | до першого пілота |
| `AMOUNT_PRESETS` і межі 20–5000 грн узяті з голови | `app/api/driver_qr.py` | після перших живих сесій |
| CSV-періоди фіксовані (сьогодні/тиждень/місяць), без довільного діапазону | `app/handlers/operator_billing.py` | якщо оператори попросять |
| ~~Станція без геолокації~~ | — | **закрито Промптом 4c** — крок геолокації в майстрі станції |
| Фільтрів «швидкі/повільні» у водійському пошуку немає — лише бейджі на картках | `app/handlers/user.py` | якщо видача виросте з 3–5 до десятків станцій |
| Радіус пошуку операторських станцій (`OPERATOR_SEARCH_RADIUS_KM = 30`) — константа, не адаптивний і не з налаштувань оператора | `app/handlers/user.py` | якщо стане тісно/рідко в різних містах |
| Для `wallet_topups` немає звірки (як `reconcile_operators.py` для `operator_payments`) — тихі `amount_mismatch` і застряглі pending ніхто не моніторить | `app/api/wallet_webhook.py` | перед реальними обсягами продажів (варіант B рев'ю) — окремим бандлом |
| `failure`/`expired`/`reversed` для wallet-поповнень живцем не перевірені (як і для станцій) | `app/api/wallet_webhook.py` | при першому живому провалі/поверненні оплати |
| Реєстр `_active_charge_points` (OCPP) — in-memory, припускає ОДИН процес uvicorn; за кількох воркерів команди 3b (RemoteStart) не знайдуть станцію на іншому воркері | `app/api/ocpp_ws.py` | коли 3b слатиме команди І якщо перейдемо на multi-worker |
| Політика дублю-підключення того ж `cp_id` (зараз обидва read-цикли працюють паралельно) — у 3b варто закривати старе з'єднання на нове | `app/api/ocpp_ws.py` | у 3b |
| ~~nginx WS-проксі на `/ocpp` (Upgrade/Connection) — ручний крок на сервері~~ | сервер (не код) | **закрито 2026-07-24** — `location /ocpp`, `Upgrade`/`Connection`, timeout 3600s; підтверджено живим `wss://`-смоуком |
| **OCPP-сесія на рівні СТАНЦІЇ, не конектора** (`start_ocpp_transaction` + індекс; `operator_sessions` без `connector_id`) — мультиконекторна станція з 2 одночасними сесіями загубить енергію 2-го конектора (знахідка рев'ю 3b, коментар у коді). **Пріоритет піднято 2026-07-28** (знахідка живого смоуку самообслуговуваного онбордингу): раніше цей ризик ловила людина, що вручну модерувала кожного оператора; тепер оператор із двоконекторною зарядкою реєструється й активується САМ, ніхто не спитає про кількість конекторів — тиха втрата кВт·год другого конектора стає реальнішою, не гіпотетичною. Для QR-оплат обхід є (дві «станції» на одне залізо), для OCPP — немає | `app/database/operators_repo.py` | ~~**Дешеве пом'якшення ДО повного фіксу**~~ — **виконано**: текст на кроці вибору конектора майстра станції («Одна станція = один конектор…») уже в коді (`app/handlers/operator_billing.py`, крок `waiting_for_connector`). **Повний фікс лишається відкритим** (окремий бандл): `operator_stations.connector_id` (або окрема таблиця конекторів) + `operator_sessions.connector_id` + дедуп/унікальний індекс по `(station_id, connector_id)` |

## Беклог

**Процесне спостереження (02-03.08.2026):** за два дні ДВІЧІ трапилось те
саме — дія виконана, а запис у цьому документі лишався застарілим (диск
почищено 30.07, запис лишався «77%»; попередження про конектор
реалізоване, запис лишався «кандидат у наступний бандл»). Другий випадок
ледь не спричинив дублювання роботи: рев'ю почало специфікувати вже
зроблене й зупинилось лише тому, що звірило з кодом. **Правило: коли
бандл закриває пункт беклогу, запис оновлюється ТИМ САМИМ бандлом, не
«потім».**

**Черга робіт (актуалізовано 2026-07-30):** ~~(1) `.gitattributes` + renormalize~~
— **зроблено** (розділ 5, CRLF/EOL); ~~(3) перевірка `user_id` у флоу «Банки»~~
— **закрито видаленням флоу** (`chore/remove-jar-flow`, 2026-07-28, розділ 5) —
флоу був тестовим, замінений живим Monobank Acquiring; ~~(2) Модель B (3c-ii) —
живий смоук-тест Monobank hold→finalize / hold→cancel~~ — **гейт ПРОЙДЕНО живим
смоуком 28-29.07.2026**, план узгоджено (`docs/plan-3c-ii.md`, три раунди рев'ю
плану), **РЕАЛІЗАЦІЯ ЗАВЕРШЕНА, ЗМЕРЖЕНО Й ЗАДЕПЛОЄНО НА ПРОД** (гілка
`feature/model-b-uah-hold`, 697 passed, 7 skipped) — три раунди рев'ю коду:
перше знайшло 1 блокер (звірка не бачила застряглі `active` uah-резервації),
закрито новим кроком звірки; **друге знайшло 1 блокер, закрито:** сам
новододаний крок (`_reconcile_stale_active_uah`) і сусідній крок
(`_reconcile_stale_pending_uah`) дзвонили `finalize_invoice`/`cancel_invoice`
БЕЗ попередньої атомарної локальної заявки (`claim_reservation_for_settlement`)
— той самий клас гонки, від якого `'settling'`-мʼютекс і вигадувався (станція,
що повертається з офлайну й шле накопичений `StopTransaction`, могла
спричинити подвійний дзвінок у банк паралельно з живим шляхом). Фікс:
`claim_reservation_for_settlement()` параметризовано (`expected_status`,
дефолт `'active'`, тепер приймає й `'pending'`), обидва кроки звірки беруть
заявку перед будь-яким мутуючим дзвінком у банк і пропускають рядок при
програші; **третє — без зауважень.** Один коміт на весь бандл (`bf35ae1` →
`main` `3b2d401` після rebase, гілку видалено локально й на GitHub) —
**ЗАДЕПЛОЄНО на прод 2026-07-29 ~20:50 UTC**: Alembic 0017→0018 підтверджено
логом деплою й живою схемою (`\d charging_reservations`: 4 CHECK, 4 нові
колонки, `user_id`/`reserved_kwh` nullable, частковий унікальний індекс на
`invoice_id`), крон `reconcile_charging_reservations.py` — 5 прогонів
00:30–04:30 UTC, усі 0/0, без `Unclosed`-шуму. **Живий смоук самого
hold→finalize/hold→cancel через Модель B — ПРОЙДЕНО 2026-07-30** (наскрізно,
реальний вебхук + production-код, реальні гроші): `destination` водієві
видно (гіпотеза підтверджена); over-capture і подвійний finalize банк
відхиляє (обидва тепер факт, не припущення) — деталі, дві нові знахідки
звірки й закриття 4 пунктів беклогу нижче → розділ «Зроблено». Узгодження
схеми й журналу `kw_transactions` (та частина п.3, що лишилась) — розділ 5
нижче, Модель B цим не заблокована.

**1. Деплой білінгу на прод** — **зроблено 2026-07-21**

- [x] `ENCRYPTION_KEY` у `.env` — команда генерації в `README.md`
- [x] `PUBLIC_BASE_URL` у `.env`
- [x] `alembic upgrade head` → `0011_operator_payments`
- [x] Перевірити лог старту: WARNING про ключ має зникнути
- [x] Перед дописуванням у `.env`: `tail -c1 .env | xxd` (інцидент 2026-07-17)

**2. Monobank схвалив заявку — зроблено 2026-07-22**

- [x] Отримати токен мерчанта, зашифрувати, зберегти оператору №0
- [x] Провести одну реальну сесію на малу суму: QR → оплата → webhook → пуш → підтвердження → чек — сесія #1, 20 грн
- [x] Звірити фактичні відповіді банку з тим, що імітує `mock_monobank.py`, і виправити мок за реальністю — `fix/mock-vs-reality`, розбіжностей рівня бага не знайдено
- [ ] Звірити форму відповіді для `failure`/`expired`/`reversed`, коли трапиться перший живий провал оплати (мок для цих статусів поки не перевірений)

**3. Безпека й інфраструктура — основне зроблено 2026-07-22**, деталі → [`docs/INFRA-AND-SECURITY.md`](INFRA-AND-SECURITY.md)

- [x] Домен + HTTPS: `chargebot.com.ua`, nginx + Let's Encrypt, `PUBLIC_BASE_URL` оновлено
- [x] **Ротація секретів:** `BOT_TOKEN`, `POSTGRES_PASSWORD`, `GEMINI_API_KEY`, `OCM_KEY`, `OCPI_SECRET_TOKEN`
- [x] Закритий зовнішній доступ до postgres/redis/bot — порти прив'язані на `127.0.0.1`
- [x] ~~Перевірити, чи `/docs` (Swagger) не публічний у проді~~ — **перевірено 03.08.2026: БУВ публічний.** `https://chargebot.com.ua/openapi.json` віддавав валідну OpenAPI-специфікацію з повним переліком ендпоінтів, включно з трьома грошовими вебхуками (`/webhook/operator/{operator_id}`, `/webhook/wallet/{operator_id}`, `/webhook/charging-hold/{operator_id}`) і OCPI-колбеками з `{user_id}` у шляху. Не вразливість сама по собі (вебхуки не вірять тілу, невідомий invoiceId → тихий 200, OCPI має токен), але безкоштовна розвідка структури API — пряме протиріччя з принципом «ендпоінт не має бути оракулом». **Закрито `fix/disable-public-api-docs`**: прапорець `ENABLE_API_DOCS` (дефолт ВИМКНЕНО, той самий ідіом що `TELEGRAM_PAYMENTS_ENABLED`) вимикає `docs_url`/`redoc_url`/`openapi_url` разом. Після деплою перевірено ЗЗОВНІ: `/openapi.json`, `/docs`, `/redoc` → 404; `/health` → 200 `{"status":"ok","checks":{"postgres":"ok","redis":"ok"}}`
- [x] **GitHub Secret scanning — перевірка 01.08.2026** (деталі → «Зроблено», «Бар'єр проти секретів у комітах»). Рівно 2 алерти, обидва з давно закоміченого `.env`, обидва вже Revoked (`GEMINI_API_KEY`, `BOT_TOKEN` — ключі перевипущені раніше, це підтвердження постфактум). Три нові пункти з цього ж аудиту:
  - [ ] **Поріг `MIN_SENSITIVE_VALUE_LENGTH = 20` у `scripts/check_secrets.py` пропускає паролі 12–19 символів** — саме такої довжини був `POSTGRES_PASSWORD` у витеклому `.env`. Знизити поріг до 12, але ЛИШЕ для імен, що містять `PASSWORD` (не для `KEY`/`TOKEN`/`SECRET` — там довші форми, нижчий поріг там підвищить хибні спрацювання без потреби). Рев'ю раунду 3 заміряло САМЕ цей вужчий варіант (поріг 12 лише для імен із `PASSWORD`): 0 хибних спрацювань на всіх 166 трекнутих файлах — зміна безкоштовна. Загальний поріг 12 для всіх чутливих імен НЕ вимірювався і не пропонується. Окремий малий бандл, не цей
  - [x] ~~**`PAYMENT_PROVIDER_TOKEN` — єдиний секрет із того самого витеклого `.env`, якого немає в списку ротованих 22.07**~~ — **рішення власника 03.08.2026: НЕ ротувати й не відключати провайдера.** Обґрунтування (міркування, не перевірений факт): провайдерський токен Telegram Payments прив'язаний до бота й використовується лише разом із `BOT_TOKEN` у `sendInvoice`, а `BOT_TOKEN` ротовано 22.07 — тож витеклий провайдерський токен сам собою малокорисний. Повернутись, якщо колись вмикатимеш `TELEGRAM_PAYMENTS_ENABLED`
  - [ ] `actions/checkout@v4` і `actions/setup-python@v5` у `.github/workflows/ci.yml` працюють на Node 20, який GitHub уже визнав застарілим (примусово підмінює на Node 24 під капотом) — оновити версії дій при нагоді, не термінове
- [x] ~~Перезавантаження сервера через оновлення ядра (некритично)~~ — **зроблено 2026-07-30**. Причина в старому формулюванні була НЕВІРНА: ядро оновлювати не було потреби, система вже працювала на найновішому встановленому (`6.8.0-136`, перевірено `uname -r` проти `/boot/vmlinuz-*`). Справжня причина — `libc6` (`/var/run/reboot-required.pkgs`, прапорець від 2026-07-29 06:38): автоматичні оновлення пропатчили glibc на диску, але процеси, запущені раніше, тримали в памʼяті стару версію. `needrestart` при оновленні перелічив їх поіменно: `dbus`, `docker.service`, `getty`, `serial-getty`, `systemd-logind`, `unattended-upgrades`. Після ребута: 0 доступних оновлень, прапорця немає, три контейнери піднялись самі (`restart: always`) і всі `healthy`, `CREATED` не змінився (том Postgres недоторканий), порти лишились на `127.0.0.1`, крони на місці (всі три, з `-T`), лог старту чистий, Telegram відповідає (три `aiogram.event ... is handled`, ~110 мс). 4 «security updates» з привітання лишаються недоступними — вони під ESM Apps (Ubuntu Pro), окреме рішення

**4. Наступні промпти**

- [x] ~~**Промпт 3 — OCPP 1.6J (фази 3a/3b/3c-i/3c-ii).**~~ **3a/3b/3c-i + бот-адмінкоманди `/ocpp_start`/`/ocpp_stop` — ЗРОБЛЕНО й ЗАДЕПЛОЄНО на прод 2026-07-24** (модель A: резерв→факт→звільнення на kWh-балансі, тригер — IN-PROCESS адмінкоманди; nginx `/ocpp` готовий, живий wss-смоук і повна валідація моделі A пройдені). **3c-ii (модель B, грн через Monobank) — ЗРОБЛЕНО, ЗМЕРЖЕНО Й ЗАДЕПЛОЄНО на прод 2026-07-29 ~20:50 UTC** (коміт `3b2d401`, 697 passed, 7 skipped, три раунди рев'ю коду — два грошові блокери закрито, третій раунд без зауважень) — план `docs/plan-3c-ii.md`, гейт живого смоук-тесту мока пройдено 28-29.07.2026 ще до реалізації; живий смоук самої Моделі B на проді — у беклозі (див. «Зроблено»/«Беклог»).
- [x] ~~**Промпт 4 — кабінет оператора.**~~ Змержено 2026-07-21, PR #13 — онбординг, еквайринг-токен, майстер станції з QR-PNG, тарифи, виручка, CSV
- [x] ~~**Міні-фіча — активація оператора кнопкою.**~~ Змержено 2026-07-21, PR #14, рев'ю пройдено
- [x] ~~**Промпт 4c — станції операторів у водійському пошуку.**~~ Змержено 2026-07-21, PR #15, докочено на прод і перевірено наживо
- [x] ~~**Промпт 5 — звірка й алерти.**~~ Змержено PR #16 — `reconcile_operators.py`, добрав обидва відкладені вікна Промптів 2a/2b. **На проді** (перезбірки 2026-07-23), звірка перевірена наживо, **cron щогодини налаштований 2026-07-23**
- [ ] **Фаза 6a kWh-гаманця — реальні оплати за кВт.** Дизайн у `kwh-wallet-design.md`. Передумови: юридичний договір/фіскалізація (поза кодом), Промпт 5 готовий. Технічна розвилка: перевикористати Monobank-еквайринг (вже працює) замість живого Telegram Payments провайдера — деталі → `docs/INFRA-AND-SECURITY.md`, §5

**5. Технічний борг (не блокує)**

- [ ] Міграція `0007` порожня — таблиці OCPI створюються лише бутстрапом, `alembic upgrade head` на чистій базі їх не зробить
- [x] ~~`feature/white-label` і `feature/qr-flow`~~ — видалені 2026-07-21
- [ ] `feature/ai-agent-setup` — `docs/` звідти вже в `main`, лишився тільки `ai_agent/`
- [ ] `reconcile_operators.py` не закриває `aiohttp.ClientSession`/конектор перед виходом — на кожному прогоні (тепер щогодини під cron) сипле `ERROR:asyncio:Unclosed client session` / `Unclosed connector` у лог. Не баг даних (робота виконується, exit 0), лише шум у логах — обгорнути сесію в `async with` або додати `await session.close()` перед виходом (виявлено при живій перевірці 2026-07-23)
- [x] ~~Крон для `reconcile_charging_reservations.py` (як `reconcile_operators`/`reconcile_payments`)~~ — **налаштовано на проді 2026-07-25**: `30 * * * *` (свідомо на :30, щоб не збігатись у часі з `reconcile_operators` на :00), повний шлях `/usr/bin/docker`, як у наявних crontab-записах; окремий лог `/var/log/ev-charge-bot-reservations.log`. Перший автоматичний прогін 2026-07-25 04:30 UTC — чисто, 0/0, без `Unclosed`-шуму (скрипт не ходить у банк, на відміну від `reconcile_operators.py`)
- [x] ~~Модель B (3c-ii, грн через Monobank hold/finalize/cancel) — ПЕРЕД кодом живий смоук-тест hold→finalize і hold→cancel (cancel на нефіналізованому hold не підтверджений документацією)~~ — **гейт ПРОЙДЕНО живим смоуком 28-29.07.2026** (реальні гроші, власна картка → власний мерчант, дві оплати по 20 грн): cancel на НЕфіналізованому hold ПРАЦЮЄ, підтверджено з обох боків (картка + виписка мерчанта). Деталі, вхідні вимоги до реалізації 3c-ii і продуктове обмеження → розділ «Зроблено» вище. Наступний крок — специфікація й реалізація 3c-ii
- [x] ~~Деплой OCPP (3a/3b/3c-i) на прод~~ — **зроблено 2026-07-24**: nginx WS-проксі `/ocpp`, alembic `0011`→`0016`, живий wss-смоук + повна валідація моделі A
- [ ] Перевитрата (`kwh > reserved`) не стягується автоматично — лише ERROR-лог; продумати донарахування для реального заліза
- [x] ~~**Перевірити over-capture живим смоуком** (`finalize` на суму БІЛЬШУ за утримане)~~ — **ПЕРЕВІРЕНО живим смоуком 2026-07-30: банк НЕ дозволяє.** HTTP 400, `{"errCode":"1001","errText":"finalization amount exceeds hold amount"}`, hold лишається цілим. Отже реалізація 3c-ii (капувати `finalize` на утримане, варіант 1) — ЄДИНО МОЖЛИВА; пункт «якщо банк дозволить — спростити `compute_uah_settlement_amount()`» закрито як неактуальний (донарахування перевитрати можливе лише окремим новим інвойсом — окреме продуктове рішення, не спрощення поточної функції). Коментар-«ПРИПУЩЕННЯ» в `mock_monobank.py::finalize_invoice` і `app/services/monobank_acquiring.py::finalize_invoice()` замінено на посилання на цей живий факт
- [x] ~~**Перевірити подвійний finalize живим смоуком**~~ — **ПЕРЕВІРЕНО живим смоуком 2026-07-30: банк відхиляє.** Повторний `finalize` того самого інвойсу → HTTP 400, той самий `errCode "1001"`, `errText: "order on hold not found"`. Власний атомарний мʼютекс (`claim_reservation_for_settlement()`, статус `'settling'`) лишається ГОЛОВНИМ захистом (принцип «не покладатись на банк» не змінюється) — невідомої поведінки більше немає
- [x] ~~**Живий смоук самої Моделі B на проді**~~ — **ПРОЙДЕНО 2026-07-30**, наскрізно: реальний `/webhook/charging-hold/{operator_id}` (не мок), реальний production-код (`app/api/charging_hold_webhook.py`, `app/services/ocpp_charging.py` uah-гілка), реальні гроші. hold→finalize (50→35 грн, ~3с від оплати до фіналізації) і hold→cancel (станція офлайн → негайний `cancel_invoice()`) — обидва щасливі шляхи підтверджено; додатково впіймано живий вебхук-шторм (≥3 дублікати `hold`) і транзитний `processing`-стан. Деталі → розділ «Зроблено»
- [x] ~~**Чи бачить водій поле `destination` на сторінці оплати hold-інвойсу**~~ — **ПІДТВЕРДЖЕНО живим смоуком 2026-07-30: так.** На сторінці pay.monobank.ua текст «Зарядка …: гарантія N грн, спишеться лише спожите» показаний прямо під сумою до вибору способу оплати. Що показує шторка Apple/Google Pay — не перевірялось, але сторінку водій бачить у будь-якому разі
- [ ] **Перевитрата в Моделі B (грн) капується мовчки — оператор цього НІКОЛИ не побачить** (знахідка рев'ю реалізації 3c-ii): `compute_uah_settlement_amount()` (`app/services/ocpp_charging.py`) при капуванні пише лише `logger.error(...)` — рядок у файловому логу сервера, який оператор не читає й нічого про нього не знає. На відміну від «застряглих» резервацій (awaiting_hold/pending/active/settling), капована перевитрата НЕ проходить через жоден крок звірки й НЕ потрапляє в підсумок `LOGS_CHAT_ID` — це не помилка обробки, а нормальний (хоч і небажаний) результат нормального шляху `on_stop_transaction`/кроку 3 звірки, тож наявний механізм алертів звірки її не бачить. Свідомо не виправлено в цьому бандлі: `compute_uah_settlement_amount()` — навмисно ЧИСТА функція (без I/O, без контексту сповіщень), протягувати через неї Telegram-контекст (chat_id, ідентифікатор оператора для форматування) зіпсувало б саме те, що робить її легко тестованою і спільною для `on_stop_transaction` і звірки. Варіанти для майбутнього бандла: (а) виклики-обгортки (`on_stop_transaction`, кроки 3/4 звірки) самі порівнюють вхідний факт із `reserved_uah` ДО виклику чистої функції і штовхають алерт у `LOGS_CHAT_ID`, якщо капування відбулось; (б) `compute_uah_settlement_amount()` повертає структуру `(amount, was_capped: bool)` замість голого `Decimal`, і кожен виклical сам вирішує, що робити з `was_capped`. Пов'язано з пунктом «перевірити over-capture живим смоуком» вище — якщо колись зʼясується, що банк дозволяє finalize понад hold, капування (і потреба в цьому алерті) може взагалі зникнути
- [x] ~~**Крок 1 звірки не переживає гонку з транзитним `processing`**~~ — **закрито `feature/reconcile-transient-states`** (план — `docs/plan-reconcile-transient-states.md`, рев'ю плану Opus пройдено з однією обов'язковою правкою): `_reconcile_stale_awaiting_hold()` тепер ЧОТИРИ гілки замість двох — `hold`/`success` → алерт (свій `reason` на кожен, `success` — банк уже списав кошти повз наш облік, найгучніший сценарій кроку); `expired`/`failure`/`reversed` → термінально, `'expired'` локально (`reversed` додано рев'ю плану — банк САМ дійшов до автоскасування, без цієї гілки рядок випав би з видимості НАЗАВЖДИ); `created`/`processing`/незнайоме → транзитний стан, рядок НЕ ЧІПАТИ, лише `stats.awaiting_hold_transient` для видимості в консолі. Наявний тест, що кодував стару поведінку (`processing` → `expired`), замінено на сценарій із дійсно термінальним статусом; 10 нових тестів
- [x] ~~**Else-гілка кроку 4 звірки (`_reconcile_stale_settling`) невидима для оператора/адміна**~~ — **закрито тим самим бандлом**: неочікуваний стан банку тепер, крім `logger.error`, додає запис у `stats.alerts` (той самий формат полів, що крок 1) — потрапляє в Telegram-підсумок і exit code
- [ ] **Поріг ескалації для «вічно завислого» `processing`/`created` у кроці 1 звірки — свідомо НЕ додано** (відкрите рішення з плану `docs/plan-reconcile-transient-states.md`, затверджено варіант А при рев'ю плану): якщо банк ніколи не доходить ні до `hold`, ні до термінального статусу, рядок так і буде мовчки пропускатись (`stats.awaiting_hold_transient`) кожним прогоном без кінця. Не додано зараз — немає живого спостереження зависання довше секунд/хвилин (найдовше в смоуку 30.07.2026 — секунди), довільний поріг (напр. «6 годин») був би константою без емпіричної підстави. Додати `AWAITING_HOLD_TRANSIENT_ALERT_HOURS`-подібний поріг → алерт (не мутуючи резервацію), якщо колись підтвердиться живцем
- [x] ~~CRLF/EOL: додати `.gitattributes` (`* text=auto eol=lf`) + renormalize~~ — **зроблено `chore/gitattributes-eol`** (`* text=auto eol=lf`, `*.sh text eol=lf`, бінарні розширення про запас). Локальний `core.autocrlf=input` (виставлений 2026-07-25 як тимчасове знеболювальне) БІЛЬШЕ НЕ ПОТРІБЕН — `.gitattributes` має пріоритет над `core.autocrlf`; власник може зняти налаштування сам після мержу. Робоче дерево свідомо ЛИШАЄТЬСЯ CRLF на Windows — переписувати його не треба, `text=auto` нормалізує при порівнянні, фантомних діффів більше не буде. `--renormalize` несподівано не зачепив жодного файлу, крім самого `.gitattributes` — індекс уже зберігав LF для всіх 139 текстових файлів (`git ls-files --eol` підтвердив `i/lf` скрізь), тож коміт вийшов чистий (1 файл, 18 рядків), без очікуваних ~138 файлів шуму
- [x] ~~`.claude/` не в `.gitignore`~~ — додано в бандлі бот-адмінкоманд (2026-07-24)
- [ ] Модель ролей/доступу в боті («тільки клієнт / клієнт+оператор / оператор») — окремий малий бандл; ролі: `users`=клієнт, `operators` через `get_operator_by_telegram_id`=оператор; варіанти: перемикач у меню / per-handler guard / allowlist. Уточнити мету перед стартом
- [ ] (дрібне) enum-значення `transaction_type` `'hold'`/`'release'` продубльовані міграцією+бутстрапом, але без окремого schema-drift тесту саме на значення enum
- [x] ~~`app/services/ocpp_charging.py::start_charging_session()` ловить лише `ChargePointNotConnected` між hold і RemoteStart~~ — **закрито `feature/ocpp-hold-hardening`** (див. «Зроблено»): пост-резерваційна логіка обгорнута в `try/except BaseException` + `asyncio.shield()` на release
- [x] ~~Живий смоук `/ocpp_start`→`/ocpp_stop` ПІСЛЯ деплою герметизації hold~~ — **проведено 2026-07-25** (див. «Зроблено», станція `SMOKE-3C-2`, два послідовні цикли, `operator_session_id`↔`ocpp_transaction_id` не переплуталися)
- [ ] (опц.) `/ocpp_cps` — адмінкоманда: список станцій, зараз підключених до `_active_charge_points` цього процесу — зручно для діагностики без прямого доступу до логів/БД
- [ ] Драйвер-фейсинг флоу для 3c-i (Telegram UI поверх `create_charging_reservation`/`app/services/ocpp_charging.py`, вибір станції водієм, показ QR/статусу) — зараз тригер лише адмінський (`/ocpp_start`/`/ocpp_stop`), без UI бота
- [ ] **Розбіжність міграції та бутстрапу по `kw_transactions`** (знахідка живого смоуку 2026-07-25, НЕ баг цього бандла): `migrations/versions/b1b193e2bd7b_initial_schema.py` створює `user_id BIGINT NOT NULL` без FK і `amount NUMERIC(8,2)`; бутстрап `app/database/connection.py` — `user_id BIGINT REFERENCES users(user_id) ON DELETE CASCADE` і `amount NUMERIC(10,2)`. Перевірено на проді 2026-07-25: `pg_constraint` для `kw_transactions` містить лише `pkey` і `payment_id_fkey` — прод має версію МІГРАЦІЇ (без FK на `users`). Наслідки: (а) видалення рядка з `users` лишає осиротілі рядки журналу — виявлено при прибиранні даних смоуку: у водія `999000001` `users.balance` показував 3.00, а `SUM(kw_transactions.amount)` — 8.50; місця, що рахують баланс як `SUM` (`app/services/ocpi/commands_service.py`, `app/handlers/ocpi_stations.py`), побачили б 8.50; (б) локальна схема (бутстрап) і прод (міграції) поводяться по-різному — той самий клас, що баг writable-CTE у PR #25. Наявний schema-drift тест перевіряє лише ПРИСУТНІСТЬ колонок, тож обмеження й типи не покриті за побудовою. Закривати окремим бандлом: міграція, яка спершу чистить осиротілі рядки, потім додає FK і вирівнює `NUMERIC`, плюс розширення drift-тесту на constraints і типи колонок. Пріоритет: не блокер (застосунок ніде не видаляє `users`), але перед Моделлю B варто вирішити — там додається ще один тип руху грошей
  - **Дорозслідування завершено 2026-07-25 (осад, не активна діра):** осиротілих рядків журналу — 1 (`user_id=8855895437`, deposit 50.00, 2026-07-15, «Успішна оплата пакету… через Банку Monobank»); осиротілих платежів — 1. `8855895437` — це Telegram-id САМОГО бота (`@evolt_ua_charge_bot`), не реального водія: у флоу «Банки» id водія береться з коментаря до платежу, і під час тесту в коментар потрапив id бота — не помилка коду, разова тестова подія
  - Розсинхрон `users.balance` ↔ `SUM(kw_transactions.amount)` — рівно ОДИН акаунт: `514533557` (особистий акаунт власника, створений 2026-07-14): `balance` 70.00, журнал −787.70 (`withdrawal` 9 рядків −1037.70 + `deposit` 4 рядки +250.00, усі 14–16 липня). Причина відома й уже виправлена: до фіксу, описаного коментарем у (нині видаленому, `chore/remove-jar-flow`, 2026-07-28) `app/api/payments.py`, нарахування писались прямим `INSERT` у `kw_transactions` і не оновлювали `users.balance` — лишився лише осад у даних, сам баг закритий
  - **Висновок: клієнтських акаунтів не зачеплено, активної діри немає.** Прибирання осаду (обидва рядки/акаунти) — частина того самого майбутнього бандла (міграція+FK+drift-тест вище), не терміново
- [x] ~~`app/api/payments.py`, вебхук «Банки» (`/webhook/monobank`) бере Telegram-id водія з КОМЕНТАРЯ до платежу без перевірки, що такий користувач взагалі існує~~ — **закрито видаленням, не фіксом (`chore/remove-jar-flow`, 2026-07-28).** Флоу був тестовим (перевірено окремими 4 пошуками — жодного функціонального зв'язку з живими шляхами не знайдено) і вже замінений з 2026-07-23 живим Monobank Acquiring (`app/api/wallet_webhook.py`, buy-side пакетів). Мертвий код, що рухає гроші, — найгірший вид мертвого коду (наживо не перевіряється, у рев'ю не потрапляє, ризик «увімкнути назад» через рік і отримати ту саму діру), тому не патчили перевіркою `user_id`, а прибрали цілком: `app/api/payments.py`, `test_monobank_signature.py` (5 тестів), реєстрацію роутера в `app/main.py`, `MONOBANK_API_TOKEN` з `.env.example` і `docs/PROJECT_CONTEXT.md`. Деталі, 4 перевірки перед видаленням (чи `MONOBANK_API_TOKEN`/`PRICE_PER_KWH`/`mock_monobank.py` потрібні деінде) і повний дифф → `review_prompt-remove-jar-flow.md`. 589 → 584 passed (−5, лише видалений файл тестів)
- [ ] **`commission_pct` не повинен мати DEFAULT узагалі** (знахідка рев'ю самообслуговуваного онбордингу, 2026-07-28) — грошова умова оператора має проставлятись ЯВНО при створенні, а не мовчки успадковувати дефолт (зараз 4, бізнес-рішення 2026-07-23 — 5%; `feature/self-service-onboarding` виправляє лише сам DEFAULT на 5% вперед, не бекфілить наявних операторів і не прибирає DEFAULT — окремий майбутній бандл)
- [ ] `operators.is_public` (самообслуговуваний онбординг, `feature/self-service-onboarding`) — перемикач публічної видимості в водійському пошуку існує лише як `repo.set_operator_public()`, без бот-кнопки в цьому бандлі (свідома девіація — специфікація просила кнопку лише для suspend). Додати кнопку/команду, якщо власник захоче вмикати публічність з Telegram, а не прямим викликом
- [x] ~~**Токен Monobank зберігається ДО перевірки банком**~~ — **закрито `fix/operator-ux-before-first-tenant`** (02-03.08.2026): `_verify_and_save_new_token()` питає банк ПЕРШИМ, у БД нічого не пишеться до підтвердження — див. «Зроблено»
- [x] ~~(дрібне, UX) «⏳ Заявку на розгляді»~~ — **закрито тим самим бандлом**: переформульовано на вказівку до чек-листа
- [x] ~~(дрібне) Дублювання id у сповіщенні адміну без username~~ — **закрито тим самим бандлом**
- [x] ~~(дрібне, UX) Призупиненому показується кнопка «➕ Додати станцію»~~ — **закрито тим самим бандлом**: `get_cabinet_menu(..., is_suspended)`
- [ ] (косметика) Старі картки «📋 Прогрес активації» лишаються в історії чату із застарілим вмістом після того, як крок закрито (знахідка смоуку 2026-07-28: картка о 14:50 показувала «станція ❌» вже після додавання станції о 15:01) — свіжий виклик малює правильно, дії не потребує, але оператор, гортаючи вгору, може вирішити, що щось не спрацювало
- [ ] (косметика) У оператора id=1 поле `name` містить назву разом із телефоном — артефакт раннього онбордингу (знахідка живого смоуку операторського UX, 03.08.2026). Водіям НЕ видно (перевірено: `_format_operator_station_card` рендерить лише назву станції), видно лише власнику в кабінеті й у адмін-чаті. Виправити одним `UPDATE` колись між справами
- [x] ~~**Місце на диску 77%**~~ — **вирішено: власник почистив диск 30.07.2026.** Виміряно 01.08.2026: 54% (13G з 24G, вільно 11G). Розклад Docker: образи 1.723 ГБ (4, з них 3 активні), build cache 854 МБ, томи 67 МБ (89 томів, активні 2). Реально безпечно звільняється лише build cache ~809 МБ — «97% образів reclaimable» у `docker system df` це артефакт обліку, образи запущених контейнерів prune не чіпає. Логи контейнерів перевірено окремо: найбільший 1.8 МБ, безмежного зростання нема. Повернутись ближче до 85%. `docker volume prune` не запускати без розбору — серед двох активних том Postgres із бойовими даними
- [ ] **Базові образи ніколи не освіжались** (знахідка 2026-07-30 при ребуті через `libc6`) — оновлення ХОСТА не патчить застосунок: `python:3.11-slim` має власний glibc усередині образу, і `docker compose build` бере базовий шар із кешу, якщо не сказати `--pull` явно. Тобто glibc у контейнері бота лишається тієї версії, яка була на момент першого завантаження образу. `needrestart` при ребуті це прямо підтвердив: «No containers need to be restarted» — хостовий патч їх не стосується. `postgres:16-alpine`/`redis:7-alpine` на musl, тому конкретна glibc-діра їх не торкається, але свій цикл оновлень у них теж є. Дія: періодично `docker compose build --pull`; для postgres/redis — окремо й уважніше (мінорні оновлення в межах 16.x безпечні за форматом даних, але це деплой, а не рутина)
- [x] ~~Чи шкодить, що `monobank_token_verified_at = NULL` у наявних операторів (напр. id=1, токен підключений задовго до появи колонки)?~~ — **з'ясовано смоуком 2026-07-28, дії не потребує (не борг):** екран «Прогрес активації» показується лише поки оператор `pending` — після активації кнопка з кабінету зникає, тож NULL у вже-`active` операторів нікому не видно й нічого не ламає (перевірку критеріїв автоактивації такі оператори більше не проходять, бо `try_auto_activate()` вимагає `status='pending'`)

**6. Знайдено при рев'ю Промпту 4 (не блокує PR, не робити зараз)**

- [ ] «Сьогодні» у виручці (`_period_since`, `app/handlers/operator_billing.py`) — північ **UTC**, не Києва: для оператора доба реально починається на 2–3 години раніше/пізніше, ніж показує кабінет
- [ ] CSV-експорт (`list_ledger_since`) мовчки обрізається на `limit=1000` рядків без жодної позначки в файлі чи повідомленні — додати попередження, коли рядків більше за ліміт
- [x] ~~`suspended`-оператор і далі може змінювати тариф/статус станцій і додавати нові через кабінет~~ — **закрито `feature/self-service-onboarding`**: `_is_suspended()` guard на вході в кожну мутуючу дію (токен/нова станція/тариф/статус станції); перегляд лишається доступним свідомо
- [ ] Онбординг (`/operator`, майстер станції, підключення токена) технічно доступний і в групових чатах — приватність (`chat.type == "private"`) перевіряється лише для кроку з токеном Monobank. **Свідомо не зачеплено** бандлом самообслуговуваного онбордингу (`feature/self-service-onboarding`) — не пов'язане з активацією/чек-листом, окреме продуктове рішення про групові чати команд

**7. Знайдено при рев'ю Промпту 4c (не блокує PR, не робити зараз)**

- [ ] `station_wizard_connector_preset` (callback на `opconn:`) зареєстрований лише на `StateFilter(StationWizard.waiting_for_connector)` — застарілий/повторний клік на кнопку конектора, коли оператор уже вийшов із цього кроку майстра, не потрапляє в жоден хендлер узагалі, тож `callback.answer()` ніхто не викликає, і кнопка в клієнті Telegram "крутиться" до таймауту замість миттєвої відповіді

**8. Знайдено при рев'ю TomTom-шару (`feature/tomtom-live-layer`, 2026-07-31; не блокує PR, не робити зараз)**

- [x] ~~**Успішна відповідь TomTom повністю виключає OCM — станції, яких TomTom не знає, стають невидимими.**~~ — **закрито `fix/merge-ocm-tomtom-layers`**: `handle_location` (`app/handlers/user.py`) тепер запитує OCM ЗАВЖДИ, паралельно з TomTom (exclusive-or прибрано); та сама фізична станція, що прийшла з обох джерел, схлопується в один запис новою `_dedupe_by_proximity` (поріг `DEDUPE_DISTANCE_KM=0.1`, ~100 м), з пріоритетом оператор → більше метаданих → TomTom. Покрито тестами (`_dedupe_by_proximity`/`_dedupe_winner`, і регресія на саму причину — `test_handle_location_queries_ocm_even_when_tomtom_returns_results`).
- [ ] **Правило, засвоєне цим бандлом: форму відповіді зовнішнього API звіряти ЖИВИМ запитом ДО написання мока/парсера, не після.** Перший прохід (`feature/tomtom-live-layer`) писав нормалізацію TomTom з документації — тести були зелені проти самописного мока, що повторював ті самі (хибні) припущення, що й код. Рев'ю Opus живими запитами до TomTom 31.07.2026 знайшло два реальних розходження (`chargingPark` — поле верхнього рівня `result`, не `poi`; параметр статусу — `chargingAvailability`, не `chargingAvailabilityId`), обидва мовчки ламали фічу на проді при повністю зелених тестах. Той самий клас проблеми, що PR #25 (writable-CTE) і «мок vs реальність» (`mock_monobank.py`). Застосовувати для будь-якої майбутньої інтеграції зовнішнього API: спершу один живий запит і фіксація реальної форми відповіді, потім мок/парсер під неї — не навпаки.

**9. `SEARCH_SINGLE_MESSAGE` — рішення власника від 2026-07-31 (`feature/adaptive-radius-and-cards`)**

- [ ] Живий смоук за містом показав дві проблеми одноповідомленнєвого рендеру одночасно з вузьким радіусом TomTom (15 км, поки OCM — 50 км): за містом видача лишалась зовсім без TomTom-станцій. Замість продовжувати доводити новий формат — власник вирішив ПОВЕРНУТИ дефолт на окремі картки (`SEARCH_SINGLE_MESSAGE=false` за замовчуванням), лишивши одноповідомленнєвий рендер за прапорцем (`SEARCH_SINGLE_MESSAGE=true`), не видаляючи його код. Якщо **до 2026-08-07** одноповідомленнєвий режим НЕ знадобиться (нікому не потрібно буде вмикати прапорець назад) — видалити його рендер (`_format_operator_result_entry`, `_format_tomtom_result_entry`, `_format_ocm_result_entry`, `_format_search_result_entry`, `_format_search_results_message`, `_build_attribution_footer`, `_build_keyboard_items`, `get_search_results_keyboard` в `app/keyboards/reply.py`) разом із тестами на нього й самим прапорцем `SEARCH_SINGLE_MESSAGE`, щоб не тримати два паралельних шляхи рендеру видачі водієві

**10. Знайдено живим смоуком 31.07.2026 (карта TomTom; усі видимі водієві, не блокує, не робити зараз)**

- [ ] **Бейдж суперечить конектору:** станція з CHAdeMO 36 кВт показується водієві як «🔌 Середня (AC)», хоча CHAdeMO — виключно DC-конектор. `classify_station_speed` (`app/services/station_speed.py`) вирішує ЛИШЕ за потужністю, коли вона відома (36 кВт потрапляє в діапазон «середня»), а буква «AC» додається за замовчуванням у самому тексті бейджа, не за фактичним типом струму. При цьому `_is_dc_station` (`app/handlers/user.py`, квота видачі) правильно рахує цю ж станцію в DC-квоту (бачить `current_type=="DC"`/хінт `CHAdeMO`) — тобто відбір станції в DC-квоту і текстовий бейдж на її картці зараз СУПЕРЕЧАТЬ одне одному для водія. Лікується врахуванням типу конектора (TomTom `current_type` + хінти CCS/CHAdeMO/GB-T DC) безпосередньо в самому ярлику `classify_station_speed`, не лише за порогом потужності
- [ ] **Дублікати з ОДНОГО джерела не склеюються:** у видачі під Рогатином дві картки «Рогатинська міська громада» за 8.61 і 8.62 км — та сама фізична точка (ймовірно два конектори одного майданчика, записані в TomTom як два окремих записи). `_dedupe_by_proximity` (`app/handlers/user.py`) навмисно порівнює лише РІЗНІ джерела (`existing["source"] == item["source"]` → `continue`) — дублі одного джерела свідомо поза обсягом попереднього бандла. Лікується дозволом склеювати й однакові джерела при відстані < `DEDUPE_DISTANCE_KM` (переможець — той самий алгоритм `_dedupe_winner`, метадані/спершу знайдений)
- [ ] **Адаптивний радіус (`TOMTOM_FALLBACK_RADIUS_KM=30`) ще НЕ перевірено живцем:** у смоуку під Рогатином станції TomTom були за 8.6 км — у межах основних 15 км, тож розширення радіуса не спрацьовувало жодного разу за весь смоук. Перевірити в точці, де найближчі TomTom-станції за 40+ км (та сама точка за містом, де 31.07 видача була з самого лише OCM — саме вона й стала причиною цього бандла)

**11. Eco-Movement — вимір покриття України ще НЕ зроблено**

- [ ] Безкоштовний семпл схвалено на AWS Data Exchange 31.07.2026 (доступ до 13.09.2026, два CSV, регіон EU Frankfurt). Вимір покриття України (кількість станцій/конекторів, перетин із OCM/OSM — той самий підхід, що дав OCM 572 / OSM 907 / перетин 138 / об'єднання 1341, див. «Зроблено», «Карта: живий інфошар TomTom») по цьому джерелу ще НЕ проведено. **Дані ліцензовані ЛИШЕ для оцінки — у прод НЕ йдуть за жодних обставин.** Мета виміру — суто вирішити, чи має сенс розмова з продажами Eco-Movement (платна ліцензія), не інтеграція семплу як такого

## Зовнішні фронти

Це поза репозиторієм — оновлювати руками.

| Фронт | Статус | Далі |
|---|---|---|
| Monobank еквайринг — заявка мерчанта | **схвалено** | — |
| Токен оператора №0 (пілот на собі) | **є, зашифрований, у `operators.monobank_token_encrypted`** | — |
| Дія — КВЕДи | **додані** | — |

**Критичний шлях розблоковано 2026-07-22.** Перший живий платіж пройшов
(сесія #1, 20 грн, `status=success`) — `invoice/create` і `invoice/status`
перевірені на практиці, `mock_monobank.py` звірено з реальною відповіддю
й доповнено (`fix/mock-vs-reality`). Ще НЕ перевірені живцем: форма
відповіді для `failure`/`expired`/`reversed`, терміни й поведінка
ретраїв webhook — закладати час, якщо трапляться розбіжності при першому
живому провалі оплати.

Локальний прогін флоу: `uvicorn mock_monobank:app --port 8081` +
`MONOBANK_ACQUIRING_BASE_URL=http://127.0.0.1:8081` у `.env`.
