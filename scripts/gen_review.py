#!/usr/bin/env python3
"""
Збирає review_prompt<N>.md — один файл з повним кодом усього, що зробив
черговий промпт White-Label білінгу, для рев'ю однією стрічкою.

Навіщо окремий скрипт, а не ручна збірка: код у review-файлі читається
ПРЯМО з робочої копії, тому він фізично не може розійтися з тим, що лежить
у комітах. Ручне копіювання в markdown такої гарантії не дає — і саме через
розходження двох копій однієї сутності цей проєкт уже ловив баги
(див. PROJECT_CONTEXT.md, п.8 про 'refund').

Запуск (з кореня репозиторію або будь-звідки — шлях визначається сам):
    python scripts/gen_review.py 1

Результат: review_prompt1.md у корені репозиторію. Ці файли в .gitignore
(правило review_prompt*.md) — вони дублюють коміти й швидко застарівають,
тому перегенеровуються за потреби, а не зберігаються в історії.

Щоб додати наступний промпт — допишіть запис у PROMPTS нижче. Опис до
кожного файлу пишеться руками навмисно: він пояснює РОЛЬ файлу, чого з
самого коду не видно.
"""
import subprocess
import sys
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent

PROMPTS = {
    "1": {
        "title": "Промпт 1 — Тенанти і станції",
        "base": "40e7778^",       # origin/main ДО першого коміту Промпту 1
        "tip": "66d1b3d",         # останній коміт Промпту 1
        "intro": (
            "White-Label білінг, Промпт 1 з `docs/evolt-white-label-bilinh-ta-p2p.md`:\n"
            "оператори зарядних станцій як тенанти, їхні станції, сесії зарядки та\n"
            "журнал розрахунків."
        ),
        "files": [
            (
                "migrations/versions/0010_white_label_tenants.py",
                "НОВИЙ",
                "Alembic-міграція 0010 — джерело правди для схеми. "
                "`down_revision='0008_add_refund_type'`.",
            ),
            (
                "app/database/operators_repo.py",
                "НОВИЙ",
                "Репозиторій мультитенантного білінгу + idempotent-дзеркало схеми "
                "(`init_operator_tables()`).",
            ),
            (
                "test_operator_isolation.py",
                "НОВИЙ",
                "74 тести ізоляції тенантів, ідемпотентності нарахувань і "
                "округлення комісії — на фейкових з'єднаннях, без живої БД.",
            ),
            (
                "app/main.py",
                "ЗМІНЕНО",
                "Підключення `init_operator_tables()` у FastAPI lifespan (+2 рядки).",
            ),
        ],
        # Файли, де зміна маленька відносно розміру файлу: показуємо diff, а
        # повний код ховаємо під <details>, щоб реальна зміна не потонула.
        "diff_only": ["app/main.py"],
        "sections": """## Ланцюг міграцій

Перевірено `alembic history` — голова одна, розгалуження немає:

```
<base> -> b1b193e2bd7b -> 0007_ocpi_locations_module -> 0008_add_refund_type -> 0010_white_label_tenants (head)
```

Файл названо `0010` відповідно до стратегічного документа, але в ланцюгу
ревізія йде одразу після `0008` — міграції `0009` у репозиторії немає.

## Три механізми ізоляції тенантів

1. **Репозиторій** — `operator_id` перший аргумент кожної тенант-скоупнутої
   функції й обов'язково у `WHERE`. Два параметризовані тести обходять усі
   23 тенант-скоупнуті функції; нова функція без фільтра впаде в тестах.
2. **`create_session`** не довіряє аргументу: `operator_id` береться з
   підзапиту по станції (`INSERT ... SELECT ... WHERE s.operator_id = $1`),
   тому для чужої станції сесія просто не створюється.
3. **Композитний FK** `(station_id, operator_id) -> operator_stations(id,
   operator_id)` — те саме обмеження вже на рівні БД.

## Ідемпотентність нарахувань (за результатом рев'ю)

Повторний webhook Monobank, ретрай після таймауту чи подвійне натискання
оператором не мають нарахувати дохід двічі — журнал незмінний, тож зайве
нарахування довелося б гасити ручним рядком `adjustment`.

- Частковий унікальний індекс `uq_ledger_session_income` на
  `(session_id, type)` — одна сесія дає рівно один дохід і одну комісію.
  Рядки без `session_id` (`payout`, `subscription_fee`, `adjustment`) під
  обмеження не підпадають.
- `uq_sessions_payment` — один інвойс Monobank не може бути привʼязаний до
  двох сесій.
- `add_ledger_entry()` пише з `ON CONFLICT DO NOTHING`;
  `record_session_income()` при конфлікті повертає id уже існуючих рядків
  і логує `duplicate income ignored`.

## Місця, що потребують ручного рев'ю

- **`record_session_income()`** — дохід і комісія двома рядками в одній
  транзакції, комісія від'ємна. Рахується в `Decimal` з `ROUND_HALF_UP`
  (не `round()` по float — той округлює до парного і зрізає копійку).
- **Кешованого балансу оператора немає** — `get_operator_balance()` це
  `SUM(amount_uah)` по журналу. Свідомо, щоб не повторити розсинхрон
  `users.balance` <-> `kw_transactions`.
- **`monobank_token_encrypted`** — репозиторій бачить лише зашифрований
  токен, шифрування (Fernet на `ENCRYPTION_KEY`) робить шар вище (Промпт 2).
  Виключений з усіх звичайних `SELECT`, дістається окремою функцією.

## Прогін тестів

```
pytest test_operator_isolation.py -q                  -> 74 passed
pytest -q (з плейсхолдерами CI env)                   -> 202 passed
```

Локально прогін був на Python 3.14 (єдиний у системі); цільова версія
проєкту — 3.11, її перевіряє CI.
""",
    },
    "2a": {
        "title": "Промпт 2a — Еквайринг оператора",
        "base": "4b87b63",        # origin/main після мержу Промпту 1
        "tip": "cfe26b7",
        "intro": (
            "Грошова частина QR-флоу з `docs/evolt-white-label-bilinh-ta-p2p.md`:\n"
            "шифрування еквайринг-токенів операторів, клієнт Monobank Acquiring,\n"
            "платежі водіїв і webhook підтвердження оплати. Сторінка `/s/{qr_slug}`,\n"
            "чек і пуш оператору — Промпт 2b."
        ),
        "files": [
            (
                "app/core/crypto.py",
                "НОВИЙ",
                "Fernet на `ENCRYPTION_KEY`: шифрування токенів операторів, ледача "
                "перевірка ключа.",
            ),
            (
                "app/services/monobank_acquiring.py",
                "НОВИЙ",
                "Клієнт Acquiring API: створення інвойсу й перевірка статусу "
                "токеном мерчанта-оператора.",
            ),
            (
                "app/api/operator_webhook.py",
                "НОВИЙ",
                "`POST /webhook/operator/{operator_id}` — не вірить тілу, перепитує банк.",
            ),
            (
                "migrations/versions/0011_operator_payments.py",
                "НОВИЙ",
                "`operator_payments` + перевішування `operator_sessions.payment_id`.",
            ),
            (
                "mock_monobank.py",
                "НОВИЙ",
                "Мок еквайрингу за зразком `mock_cpo.py` — без нього флоу не "
                "протестувати без реального оператора.",
            ),
            (
                "app/database/operators_repo.py",
                "ЗМІНЕНО",
                "Функції платежів (`create_operator_payment`, "
                "`get_operator_payment_by_invoice`, `set_operator_payment_status`, "
                "`attach_payment_to_session`, `get_session_by_payment`) + дзеркало "
                "схеми `operator_payments`.",
            ),
            (
                "test_operator_payments.py",
                "НОВИЙ",
                "32 тести: модель довіри webhook, ізоляція тенантів, ідемпотентність, крипто.",
            ),
            (
                "test_operator_isolation.py",
                "ЗМІНЕНО",
                "Нові платіжні функції додані в `TENANT_SCOPED_CALLS`; звірка схеми "
                "розширена на другу міграцію.",
            ),
        ],
        "diff_only": ["app/database/operators_repo.py", "test_operator_isolation.py"],
        "sections": """## Модель довіри webhook

```
POST /webhook/operator/{operator_id}
  -> invoiceId з тіла
  -> інвойс має існувати в operator_payments САМЕ цього operator_id (з URL)
  -> GET /api/merchant/invoice/status токеном оператора
  -> віримо ЛИШЕ відповіді банку
```

Тіло webhook не використовується ні для статусу, ні для суми. Причина: інвойси
створені токенами різних мерчантів, тож `x-sign` кожного підписаний ключем
свого оператора — глобальна перевірка з `app/api/payments.py` (наш власний
`MONOBANK_API_TOKEN`) для них не працює в принципі. Замість кешу публічних
ключів на кожного оператора тіло webhook зроблено нерелевантним: навіть маючи
URL і знаючи invoiceId, підробити оплату неможливо.

Невідомий invoiceId → тихий 200 без подробиць: відповідь однакова для «не
існує», «чужий оператор» і «все гаразд», щоб ендпоінт не став оракулом для
зондування.

**Перевірено мутацією:** якщо змусити код читати статус із тіла webhook,
падає рівно `test_webhook_body_claiming_success_does_not_credit_if_bank_disagrees`.

## Чому окрема таблиця, а не наявна `payments`

1. Водій **не реєструється** — у нього немає `users.user_id`, а
   `payments.user_id` оголошено `NOT NULL` у початковій схемі. Рядок для
   водійського платежу туди фізично не вставити.
2. `payments` — журнал НАШИХ поповнень балансу, його читає
   `reconcile_payments.py`. Домішування другої платіжної моделі зламало б звірку.
3. Мультитенантність: кожна таблиця білінгу має `operator_id`.

`UNIQUE (operator_id, invoice_id)` — не глобально, інакше оператор А міг би
«зайняти» ідентифікатор оператора Б.

## Захисти від подвійного нарахування

- Уже успішний платіж не чіпається, банк не смикається повторно
- `UPDATE ... WHERE status <> $3` — мʼютекс: рівно один паралельний webhook
  проводить нарахування
- Сума з банку звіряється з виставленою; розбіжність блокує нарахування
- Банк недоступний → 502, щоб Monobank повторив і оплата не загубилась

## Місця, що потребують ручного рев'ю

- **`ENCRYPTION_KEY` без ротації.** Зміна ключа робить усі збережені токени
  нерозшифровуваними. Процедури перешифрування ще немає.
- **Ледача перевірка ключа** — свідомий вибір: застосунок стартує без ключа
  (решта функціоналу від нього не залежить), лише гучний WARNING.
- **Проміжні статуси банку** (`created`/`processing`/`hold`) нічого не
  змінюють — покладаємось на наступний webhook. Якщо він не прийде, платіж
  лишиться `pending` до звірки (Промпт 5).

## Прогін тестів

```
pytest test_operator_payments.py -q                   -> 32 passed
pytest -q (з плейсхолдерами CI env)                   -> 202 passed
```

Локально прогін був на Python 3.14 (єдиний у системі); цільова версія
проєкту — 3.11, її перевіряє CI.
""",
    },
    "2b": {
        "title": "Промпт 2b — QR-сторінка водія",
        "base": "cfe26b7",        # одразу після Промпту 2a
        "tip": "3eb9c8e",
        "intro": (
            "Те, що бачать водій і оператор: сторінка оплати `/s/{qr_slug}`, чек\n"
            "і пуш оператору «Оплачено — увімкніть станцію» з кнопкою\n"
            "підтвердження. Грошова частина — Промпт 2a."
        ),
        "files": [
            (
                "app/api/driver_qr.py",
                "НОВИЙ",
                "Три ендпоінти водія: сторінка станції, створення інвойсу, чек.",
            ),
            (
                "templates/driver/base.html",
                "НОВИЙ",
                "Базовий шаблон: мобільний-перший, стилі інлайном, без зовнішніх ресурсів.",
            ),
            (
                "templates/driver/station.html",
                "НОВИЙ",
                "Тариф, статус станції, вибір суми, кнопка «Розпочати зарядку».",
            ),
            (
                "templates/driver/receipt.html",
                "НОВИЙ",
                "Чек із автооновленням у проміжних станах (meta refresh, без JS).",
            ),
            (
                "templates/driver/not_found.html",
                "НОВИЙ",
                "Спільна 404 для невідомого slug і чужої сесії — відповідь однакова.",
            ),
            (
                "app/services/operator_notify.py",
                "НОВИЙ",
                "Пуш оператору. Імпорт `bot` відкладений, щоб платіжні тести не "
                "тягнули aiogram.",
            ),
            (
                "app/handlers/operator_billing.py",
                "НОВИЙ",
                "Обробник кнопки «Увімкнув станцію» зі звіркою, що натиснув саме оператор.",
            ),
            (
                "test_driver_qr.py",
                "НОВИЙ",
                "43 тести: сторінка, суми, статус оператора, чек, ізоляція сесій, "
                "підтвердження оператора.",
            ),
            (
                "app/api/operator_webhook.py",
                "ЗМІНЕНО",
                "Виклик пуша після проведення оплати, загорнутий у try/except.",
            ),
            (
                "app/main.py",
                "ЗМІНЕНО",
                "Реєстрація `driver_router` і `operator_billing_router`.",
            ),
        ],
        "diff_only": ["app/api/operator_webhook.py", "app/main.py"],
        "sections": """## Флоу

```
GET  /s/{qr_slug}          тариф, статус, вибір суми
POST /s/{qr_slug}/start    сесія -> інвойс у мерчанті оператора -> 303 у Monobank
     (оплата)              -> webhook 2a: платіж success, сесія paid, дохід
     пуш оператору         «Оплачено 200 грн, увімкніть станцію» + кнопка
     кнопка натиснута      сесія -> charging
GET  /s/{qr_slug}/receipt/{session_id}   чек, оновлюється сам
```

## Ізоляція без автентифікації водія

Водій не реєструється — єдиний ключ доступу це `qr_slug` під QR-кодом. Тому:

- `operator_id` **ніде не приймається ззовні** — завжди береться зі станції,
  знайденої за slug;
- чек звіряє, що сесія належить **саме цій станції**: інакше, знаючи один
  slug, перебором `session_id` можна було б читати чужі сесії того ж оператора;
- 404 для чужої сесії й для неіснуючої — побайтово однакові (тест це фіксує);
- кнопку «Увімкнув станцію» може натиснути лише той Telegram-акаунт, що
  справді є цим оператором: формат `callback_data` не секрет, тому
  `operator_id` з нього звіряється з `telegram_id` того, хто натиснув.

## Чому сторінки без жодного зовнішнього ресурсу

Водій стоїть біля станції, часто на слабкому мобільному інтернеті або в
підземному паркінгу. Кожен зовнішній шрифт чи CDN — це ще один спосіб не
побачити кнопку «Оплатити». Стилі інлайном, сторінка = один запит. Проміжні
стани чека оновлюються `meta refresh`, а не JS-полінгом, щоб працювало навіть
із вимкненими скриптами. Є тест, який це стереже.

## Хто може приймати гроші

Перевіряється не лише статус станції, а й статус самого оператора: на паузі
(`suspended` — наприклад, за несплату підписки) чи з незавершеним онбордингом
(`pending`) він оплат не приймає, інакше ми зберемо гроші, за які ніхто не
відповідає. Перевірка стоїть **до** валідації суми й створення сесії, щоб не
плодити сміттєві `pending`-сесії. Водію показується той самий нейтральний
текст, що й для недоступної станції.

## Порядок операцій

Сесія створюється **до** інвойсу. Якщо банк не відповів — лишається слід
(`failed`), і це видно у звірці. Зворотний порядок дав би оплачений інвойс
без сліду в системі.

## Місця, що потребують ручного рев'ю

- **Аванс, а не післяоплата.** Водій платить обрану суму наперед; незужита
  частина повертається оператором вручну. Автоматичний часткововий рефанд
  можливий лише з реальним обліком kWh — це Промпт 3 (OCPP).
- **`AMOUNT_PRESETS` і межі 20–5000 грн** — узяті з голови, не з даних.
- **`custom_amount` має пріоритет над пресетом** — інакше водій вписав би
  своє число, а заплатив за радіокнопку, яка лишилась вибраною.
- **Пуш не може зламати платіжний флоу**: `notify_operator_paid` ковтає
  винятки, і місце виклику теж загорнуте — інакше 500 змусив би Monobank
  ретраїти вже оброблений платіж.

## Прогін тестів

```
pytest test_driver_qr.py -q                           -> 43 passed
pytest -q (з плейсхолдерами CI env)                   -> 202 passed
```

Локально прогін був на Python 3.14 (єдиний у системі); цільова версія
проєкту — 3.11, її перевіряє CI.
""",
    },
    "4": {
        "title": "Промпт 4 — Кабінет оператора в Telegram",
        "base": "826cd4a",         # origin/main перед стартом Промпту 4
        "tip": "HEAD",
        "intro": (
            "White-Label білінг, Промпт 4 з `docs/evolt-white-label-bilinh-ta-p2p.md`:\n"
            "оператор самостійно, без ручних SQL, керує кабінетом у Telegram —\n"
            "реєстрація, еквайринг, станції з QR, тарифи, виручка й CSV. Схема БД\n"
            "не змінюється — усе, що потрібно, вже було в міграціях 0010/0011."
        ),
        "files": [
            (
                "app/states/operator_states.py",
                "НОВИЙ",
                "FSM-стани онбордингу, підключення еквайрингу, майстра станції, "
                "зміни тарифу.",
            ),
            (
                "app/keyboards/operator.py",
                "НОВИЙ",
                "Inline-клавіатури кабінету: головне меню, перелік станцій, картка "
                "станції, вибір періоду виручки.",
            ),
            (
                "app/services/qr_image.py",
                "НОВИЙ",
                "Генерація QR-PNG станції (`qrcode[pil]`) окремим модулем — handler "
                "не знає про бібліотеку.",
            ),
            (
                "app/handlers/operator_billing.py",
                "ЗМІНЕНО",
                "Головний файл Промпту 4: онбординг, токен Monobank, майстер станції, "
                "дії зі станцією, виручка/CSV. Підтвердження «увімкнув станцію» "
                "(Промпт 2b) лишається без змін.",
            ),
            (
                "app/database/operators_repo.py",
                "ЗМІНЕНО",
                "`list_ledger_since()`/`get_ledger_summary()` — тенант-скоупні "
                "читання журналу за період.",
            ),
            (
                "app/keyboards/reply.py",
                "ЗМІНЕНО",
                "Кнопка «🏷️ Мій білінг» у головному меню.",
            ),
            (
                "app/main.py",
                "ЗМІНЕНО",
                "`operator_billing_router` переміщено ПЕРЕД `user_router`; команда "
                "/operator у `bot.set_my_commands()`.",
            ),
            (
                "test_operator_cabinet.py",
                "НОВИЙ",
                "56 тестів: онбординг, токен (шифрування/видалення/маскування), "
                "майстер станції, ізоляція тенантів на рівні хендлерів, чисті "
                "функції виручки/CSV, реальна генерація QR-PNG.",
            ),
            (
                "test_operator_isolation.py",
                "ЗМІНЕНО",
                "Нові функції репозиторію додані в `TENANT_SCOPED_CALLS`.",
            ),
        ],
        "diff_only": ["app/keyboards/reply.py", "app/main.py", "app/database/operators_repo.py",
                      "test_operator_isolation.py"],
        "sections": """## Чому без нової Alembic-міграції

`operators.status/phone`, `monobank_token_encrypted`, `operator_stations.*`,
`operator_payout_ledger.type` — усе потрібне для кабінету вже існувало з
Промптів 1/2a. Промпт 4 лише додає два нових ЧИТАННЯ журналу
(`list_ledger_since`, `get_ledger_summary`) — жодної нової таблиці чи колонки.

## Порядок роутерів (app/main.py) — не косметика, а вимога коректності

`user_router` закінчується хендлером-приймачем «будь-який текст без `/` ->
ШІ-чат» (`StateFilter("*")`). Якби `operator_billing_router` йшов, як раніше,
ПІСЛЯ `user_router`, жоден вільнотекстовий крок майстра кабінету (назва
станції, токен еквайрингу, тариф) до нього не доходив би — його ловив би
раніше той самий ШІ-чат. Тепер `operator_billing_router` зареєстрований
ПЕРЕД `user_router`, а кожен вільнотекстовий FSM-хендлер кабінету explicit
виключає повідомлення, що починаються з `/` (`_is_free_text()`) — інакше
`/start`, надісланий саме щоб вийти з майстра, сам перетворився б на «назву
станції» замість того, щоб дійти до `cmd_start` у `user.py`.

## Мультитенантність без повторної звірки в кожному хендлері

Нові callback_data (`opm:`/`opst:`/`oprev:`/`opcsv:`) свідомо НЕ несуть
`operator_id` всередині — лише `station_id` чи період. `operator_id` для
кожного запиту в repo береться ВИКЛЮЧНО з
`get_operator_by_telegram_id(callback.from_user.id)` того, хто натиснув, а
приналежність станції перевіряє сам `get_station(operator_id, station_id)`
(повертає `None` для чужої станції — той самий механізм, що в Промпті 1).
Це відрізняється від наявного `confirm_station_switched_on` (Промпт 2b),
де `operator_id` таки їде в callback_data і звіряється окремим рядком —
той формат уже в проді, змінювати його тут не було підстав.

## Токен Monobank: порядок дій

`save_monobank_token()`: спершу шифрування й запис у БД (якщо оператор і
токен присутні), ПОТІМ спроба видалити повідомлення — незалежно від того,
чи вдалось зберегти. Видалення обгорнуте в `try/except` і лише логує
`operator_id` (не сам токен) при невдачі: бот не завжди має право видалити
чуже повідомлення, і це не привід ламати збереження токена чи відповідь
користувачу. Підтвердження і будь-який лог показують щонайбільше останні 4
символи токена.

## FSM-дані мають бути JSON-серіалізовними

Прод використовує `RedisStorage` (REDIS_HOST заданий) — і його серіалізація
не вміє `Decimal`. Числові кроки майстра станції (потужність, тариф)
конвертуються в `float` одразу при записі в `state.update_data(...)`, а не
лишаються `Decimal` до фінального кроку. `TariffEdit` цієї проблеми взагалі
не має — там у стані зберігається лише `station_id` (int), саме числове
значення тарифу вводиться і використовується в одному хендлері.

## Місця, що потребують ручного рев'ю

- **Кабінет доступний і в `pending`.** Оператор може підключити еквайринг і
  додати станції ще до ручної активації — свідомо, щоб онбординг не чекав
  адміна. Реальні гроші від водіїв усе одно заблоковані перевіркою статусу
  оператора в `app/api/driver_qr.py` (Промпт 2b, без змін тут).
- **`_is_free_text()` виключає команди з кожного вільнотекстового кроку.**
  Без цього `/start` посеред майстра станції став би текстом станції, а не
  дійшов до `cmd_start`.
- **CSV-періоди фіксовані** (сьогодні/тиждень/місяць), без довільного
  діапазону дат — відкладено, як і в `README.md`/`SESSION_STATE.md`.

## Прогін тестів

```
pytest test_operator_cabinet.py -q                    -> 56 passed
pytest -q (з плейсхолдерами CI env)                    -> 262 passed
```

Локально прогін був на Python 3.14 (єдиний у системі); цільова версія
проєкту — 3.11, її перевіряє CI.
""",
    },
    "4b": {
        "title": "Промпт 4, міні-фіча — активація оператора кнопкою",
        "base": "d603348",         # origin/main одразу після мержу PR #13 (Промпт 4)
        "tip": "HEAD",
        "intro": (
            "Онбординг обіцяв «ми напишемо, коли акаунт активовано», але\n"
            "активація була лише ручним SQL, і жодного сповіщення оператор не\n"
            "отримував. Ця міні-фіча додає кнопку «✅ Активувати» під\n"
            "сповіщенням про нового оператора в LOGS_CHAT_ID."
        ),
        "files": [
            (
                "app/handlers/operator_billing.py",
                "ЗМІНЕНО",
                "`_notify_admins_new_operator` тепер додає кнопку; новий "
                "`admin_activate_operator` + `_is_from_admin_chat`.",
            ),
            (
                "app/keyboards/operator.py",
                "ЗМІНЕНО",
                "`get_admin_activation_keyboard(operator_id)`.",
            ),
            (
                "test_operator_cabinet.py",
                "ЗМІНЕНО",
                "7 нових тестів активації + перевірка кнопки в тесті сповіщення "
                "адмінів; `FakeMessage`/`FakeCallback`/`FakeBot` розширені "
                "(`html_text`, `chat_id`, `fail_send`).",
            ),
        ],
        "diff_only": ["app/keyboards/operator.py"],
        "sections": """## Захист: чат, а не telegram-акаунт

Єдиний свідомий виняток із правила Промпту 4 «operator_id лише з
get_operator_by_telegram_id()»: активація — адмінська дія без «свого»
оператора, якому можна довіряти. Замість звірки з telegram-акаунтом
`_is_from_admin_chat()` порівнює `int(callback.message.chat.id) ==
int(LOGS_CHAT_ID)` — саме int, бо LOGS_CHAT_ID для груп зазвичай від'ємний,
а рядкове порівняння крихкіше при випадковому форматуванні в `.env`. Формат
`callback_data` (`opadm:activate:<id>`) не вважається секретом — id
оператора й так тривіально вгадується.

## Порядок дій: статус -> сповіщення -> редагування

`admin_activate_operator`: спершу `set_operator_status(..., 'active')`,
потім спроба сповістити оператора (в `try/except`, за зразком
`notify_operator_paid` — заблокований бот НЕ скасовує вже проведену
активацію), і лише тоді редагування повідомлення в адмін-чаті (теж у
`try/except`, за зразком `confirm_station_switched_on`). Ідемпотентність —
через ту саму перевірку статусу, що і в решті кабінету: другий клік бачить
`status == 'active'` і відповідає «Уже активовано» без повторного
сповіщення.

## Ручна активація лишається робочою

Кнопка нічого не забирає в прямого `set_operator_status(id, 'active')` у
БД — просто інший шлях до того самого поля, без сповіщення оператору.

## Прогін тестів

```
pytest test_operator_cabinet.py -q                    -> 63 passed
pytest -q (з плейсхолдерами CI env)                    -> 269 passed
```

Локально прогін був на Python 3.14 (єдиний у системі); цільова версія
проєкту — 3.11, її перевіряє CI.
""",
    },
    "4c": {
        "title": "Промпт 4c — Станції операторів у водійському пошуку",
        "base": "f19ad80",         # origin/main одразу після мержу PR #14
        "tip": "HEAD",
        "intro": (
            "Пошук найближчих станцій ('Зарядка ⚡') показував лише OpenChargeMap.\n"
            "Станції White-Label операторів мають QR-флоу оплати, але в пошук не\n"
            "потрапляли. Ця частина додає геолокацію й конектор кнопками в майстер\n"
            "станції, публічний пошук operator_stations поруч і об'єднану видачу з\n"
            "бейджем швидкості (⚡/🔌/🐢) для обох джерел."
        ),
        "files": [
            (
                "app/services/geo.py",
                "НОВИЙ",
                "`haversine_km()` — чиста функція відстані, без БД/мережі.",
            ),
            (
                "app/services/station_speed.py",
                "НОВИЙ",
                "`classify_station_speed(power_kw, connector_type)` — однакова для "
                "операторських станцій і OCM.",
            ),
            (
                "app/database/operators_repo.py",
                "ЗМІНЕНО",
                "`list_public_stations_near()` — третій публічний виняток із "
                "правила ізоляції; SQL фільтрує статуси/координати, Python — "
                "радіус і сортування через `haversine_km`.",
            ),
            (
                "app/services/ocm_service.py",
                "ЗМІНЕНО",
                "Кожна станція тепер додатково несе `power_kw`/`connector_type` "
                "(найпотужніший конектор) — вхід для `classify_station_speed`.",
            ),
            (
                "app/handlers/operator_billing.py",
                "ЗМІНЕНО",
                "Майстер станції: новий крок геолокації "
                "(`waiting_for_location`), конектор — інлайн-кнопками "
                "(`station_wizard_connector_preset`/`_custom`) замість вільного "
                "тексту.",
            ),
            (
                "app/keyboards/operator.py",
                "ЗМІНЕНО",
                "`CONNECTOR_PRESETS`, `get_connector_presets_keyboard()`.",
            ),
            (
                "app/states/operator_states.py",
                "ЗМІНЕНО",
                "`StationWizard.waiting_for_location`.",
            ),
            (
                "app/handlers/user.py",
                "ЗМІНЕНО",
                "`handle_location` об'єднує OCM і операторську видачу "
                "(`_merge_search_results`), картки з бейджем "
                "(`_format_operator_station_card`/`_format_ocm_station_card`).",
            ),
            (
                "test_geo.py",
                "НОВИЙ",
                "Тести `haversine_km`.",
            ),
            (
                "test_station_speed.py",
                "НОВИЙ",
                "24 тести `classify_station_speed`: пороги 22/50, конектор, "
                "None, пріоритет потужності над конектором.",
            ),
            (
                "test_user_station_search.py",
                "НОВИЙ",
                "Змішана видача OCM+оператор, формат карток — усе на чистих "
                "функціях, без моків Telegram.",
            ),
            (
                "test_operator_cabinet.py",
                "ЗМІНЕНО",
                "Майстер станції переписаний під новий флоу (геолокація, "
                "кнопки конектора); +9 тестів.",
            ),
            (
                "test_operator_isolation.py",
                "ЗМІНЕНО",
                "`list_public_stations_near` — публічний виняток, структурний "
                "+ поведінковий тест (радіус, сортування, порожній результат).",
            ),
        ],
        "diff_only": ["app/keyboards/operator.py", "app/states/operator_states.py"],
        "sections": """## Третій публічний виняток із правила ізоляції

`list_public_stations_near(lat, lng, radius_km)` — після `get_station_by_qr_slug`
і `get_station_by_ocpp_charge_point_id`. Водій не автентифікований і не «свій»
жодному оператору, тож фільтр не `operator_id`, а географія + активність:
`WHERE s.status = 'active' AND o.status = 'active' AND lat/lng IS NOT NULL`
(SQL, дешево на малому датасеті), потім `haversine_km()` і сортування —
Python, свідомо: чисту функцію в рази легше тестувати на межових випадках,
ніж формулу, зашиту в SQL-рядок. Кожен рядок несе `operator_id`, тож
подальший перехід на `/s/{qr_slug}` знову йде тенант-скоупнутим шляхом.

## Класифікація швидкості: потужність понад усе

`classify_station_speed()`: якщо потужність відома — рішення ЛИШЕ за нею
(⚡ ≥50, 🔌 22–50, 🐢 ≤22, межі — 22 і 50 самі належать повільній/швидкій
категоріям, не середній). Конектор (`CCS`/`CHAdeMO`/`GB/T DC` -> швидка,
`Schuko`/`CEE`/`розетка` -> повільна) дивимось ЛИШЕ коли потужності немає
взагалі — це навмисно, щоб не плутати `GB/T AC` (побутовий, повільний) з
`GB/T DC` (швидкий): підрядок `"GB/T"` окремо ніде не перевіряється, лише
повний хінт. Якщо немає ні того, ні того — бейджа немає, а не вгадуємо.

## Майстер станції: конектор став фактично обов'язковим

На відміну від адреси/локації/потужності/старту (усі — «-» пропускає),
крок конектора замінено на 5 інлайн-кнопок (`Type 2`/`GB/T AC`/`Schuko`/
`CEE 5-pin (3ф)`/«Інше») БЕЗ кнопки «Пропустити» — свідома зміна поведінки
з Промпту 4 (там конектор був вільним текстом і скіпався через «-»).
Причина — саме конектор разом із потужністю формує бейдж швидкості, тож
пропускати його непомітно означало б систематично губити бейдж. «Інше»
лишає той самий FSM-стан і чекає вільний текст — `station_wizard_connector_custom`
приймає довільний рядок і туди ж потрапляє, якщо оператор проігнорував
кнопки й одразу написав текстом.

## HTML-екранування операторського тексту (за результатом рев'ю)

Назва станції, адреса, конектор і назва оператора — вільний текст, який
оператор сам вводить у майстрі/онбордингу, а рендериться він у повідомлення
з `parse_mode="HTML"`. Без `html.escape()` це давало два реальні наслідки:

1. **Непарсибельний HTML.** Символ `<` у назві ламає парсинг, і Telegram
   узагалі не надсилає повідомлення. Найгірший випадок — саме
   `_notify_admins_new_operator()`: рядок в `operators` уже записано, заявка
   «є», але `bot.send_message` падає з винятком, і адмін ніколи про неї не
   дізнається — вона фактично губиться.
2. **Лінк-ін'єкція.** `<a href=...>` у назві станції потрапляв би в публічну
   видачу водіям сирим тегом замість тексту.

`html.escape()` додано в кожному місці рендеру операторського тексту з
`parse_mode="HTML"`: `_format_operator_station_card` (`app/handlers/user.py`),
`_station_detail_text`, `_send_cabinet_home`, підпис у `_send_new_station_qr`,
`_notify_admins_new_operator` (усі — `app/handlers/operator_billing.py`).
Картка OCM лишається на `parse_mode="Markdown"` і не з операторським
текстом — поза обсягом цієї правки.

## Місця, що потребують ручного рев'ю

- **`OPERATOR_SEARCH_RADIUS_KM = 30`** (`app/handlers/user.py`) — константа
  з голови, як і `AMOUNT_PRESETS` у Промпті 2b.
- **Фільтрів «швидкі/повільні» у видачі немає навмисно** — 3–5 станцій на
  запит, бейджів на картках достатньо; у беклог, якщо видача виросте.
- **`OperatorSearch` не кешує** список операторських станцій (на відміну
  від OCM, який кешується на 300с через `@cached`) — при малій кількості
  станцій MVP це не проблема, але варто тримати в голові при масштабуванні.
- **`station_wizard_connector_preset`** реагує лише в стані
  `waiting_for_connector` — застарілий клік поза майстром лишає кнопку без
  відповіді (`callback.answer()` ніхто не викликає). У беклог, не критично.

## Прогін тестів

```
pytest test_geo.py test_station_speed.py test_user_station_search.py -q  -> 40 passed
pytest test_operator_cabinet.py test_operator_isolation.py -q            -> 157 passed
pytest -q (з плейсхолдерами CI env)                                      -> 325 passed
```

Локально прогін був на Python 3.14 (єдиний у системі); цільова версія
проєкту — 3.11, її перевіряє CI.
""",
    },
    "5": {
        "title": "Промпт 5 — Звірка операторського білінгу й алерти",
        "base": "6b24365",         # origin/main одразу після мержу PR #15
        "tip": "HEAD",
        "intro": (
            "Платіжний ланцюг Промптів 2a/2b має два свідомо відкладені вікна,\n"
            "задокументовані коментарями в коді: (1) платіж->success, сесія->paid,\n"
            "дохід — не в одній транзакції, падіння процесу лишає частковий стан;\n"
            "(2) інвойс створений у банку, а рядок operator_payments не встиг\n"
            "записатись. `reconcile_operators.py` добирає обидва — перепитує банк\n"
            "токеном оператора для завислих pending-платежів, доводить перервані\n"
            "ланцюги до кінця тим самим ідемпотентним шляхом, що й webhook, і\n"
            "алертить на розбіжності, які автоматично не виправити."
        ),
        "files": [
            (
                "app/api/operator_webhook.py",
                "ЗМІНЕНО",
                "Рефакторинг без зміни поведінки: apply_bank_status()/"
                "complete_paid_session() винесені зі handler'а — спільна логіка "
                "для webhook'а і звірки. test_operator_payments.py (32 тести) "
                "проходить без жодної зміни.",
            ),
            (
                "app/database/operators_repo.py",
                "ЗМІНЕНО",
                "4 нові тенант-скоупні читання для звірки + list_operators() "
                "— четвертий документований виняток із правила ізоляції.",
            ),
            (
                "reconcile_operators.py",
                "НОВИЙ",
                "Скрипт звірки: 4 сценарії розбіжностей, цикл по операторах, "
                "підсумок у консоль і LOGS_CHAT_ID.",
            ),
            (
                "test_reconcile_operators.py",
                "НОВИЙ",
                "16 тестів: усі 4 сценарії, ідемпотентність повторного прогону, "
                "ізоляція операторів, недоступний банк одного не зупиняє решту, "
                "пуш підсумку в Telegram.",
            ),
            (
                "test_operator_isolation.py",
                "ЗМІНЕНО",
                "4 нові функції репозиторію додані в TENANT_SCOPED_CALLS.",
            ),
        ],
        "diff_only": ["app/database/operators_repo.py", "test_operator_isolation.py"],
        "sections": """## Чому webhook рефакториться саме зараз

Три кроки, які Промпт 2a лишив без спільної транзакції (платіж->success,
сесія->paid, дохід), звірці треба відтворити один в один — інакше вона
знаходила б розбіжності по одній логіці, а виправляла по іншій, і рано чи
пізно вони розійшлися б. `apply_bank_status()`/`complete_paid_session()`
винесені з `operator_invoice_webhook` у той самий модуль (не в новий
файл) — саме тому, що існуючі тести монкіпатчать `operator_webhook.
get_invoice_status`/`operator_webhook.notify_operator_paid` за іменем
модуля; винос в окремий сервісний файл зламав би ці монкіпатчі мовчки
(нова функція зверталась би до СВОЄЇ копії імені, а не до
пропатченої). Рефакторинг поведінково ідентичний — увесь
`test_operator_payments.py` пройшов без жодної правки в самому тесті.

## Четвертий виняток із правила ізоляції: list_operators()

На відміну від `get_station_by_qr_slug`/`get_station_by_ocpp_charge_point_id`/
`list_public_stations_near` (публічні, для НЕавтентифікованого водія),
`list_operators()` — адмінський/cron-інструмент: звірка за визначенням
мусить пройти по ВСІХ тенантах, а не по одному. Секретів не повертає
(`_OPERATOR_FIELDS` без `monobank_token_encrypted`, як і всюди); токен
кожного оператора звірка й далі дістає окремим тенант-скоупним викликом
`get_operator_monobank_token_encrypted(operator_id)`.

## Один недоступний банк не валить весь прогін

Токен оператора не розшифровується або банк відповідає помилкою —
оператор пропускається з алертом (`stats.bank_unavailable`), а звірка
йде далі по решті операторів. Перевірено окремим тестом
(`test_bank_down_for_one_operator_does_not_block_others`): оператор Б
звіряється й виправляється навіть коли токен оператора А зламаний.

## Ідемпотентність — не декларація, а тест

`test_second_run_does_not_double_credit_anything` запускає `run()` двічі
поспіль на одному й тому самому стані й звіряє журнал і лічильник
сповіщень побайтово. Це працює "безкоштовно": сама звірка не містить
жодної спеціальної логіки "не роби вдруге" — ідемпотентність цілком
успадкована від `record_session_income()` (частковий унікальний індекс)
і мʼютексного `UPDATE ... WHERE status <> $3` у `set_operator_payment_status`,
обидва з Промпту 2a. Звірка лише знаходить, до чого їх повторно застосувати.

## Тестування часу без залежності від реального годинника

`run()` приймає необов'язковий `now` — тести фіксують його константою,
щоб порівняння з `INVOICE_TTL_SECONDS`/годинним порогом не залежало від
того, коли саме запуститься pytest (перша версія тестів мала прихований
баг саме на цьому: захардкоджена дата в тесті й `datetime.now()` у
скрипті — різні моменти часу, тест був недетермінованим).

## Пуш у Telegram: залежність винесена в `_get_bot()`

`app.core.loader` створює `Bot()` на імпорті модуля й падає без валідного
`BOT_TOKEN` — а тестове середовище цього проєкту (без `.env`) його не
має. `_get_bot()` — однорядкова обгортка над `from app.core.loader import
bot`, яку тести підміняють напряму (`FakeBot`, або функція, що кидає
виняток) без жодного реального мережевого виклику чи залежності від
порядку імпорту тестових модулів у сесії pytest. Збій пуша логується й
не впливає на exit code — його визначають реальні розбіжності.

## Місця, що потребують ручного рев'ю

- **Сценарій "банк і після TTL каже processing/hold/created"** —
  трактується як `needs_manual` (стан `unknown_status` у
  `apply_bank_status`), а не як помилка чи авто-фікс: на практиці такого
  не мало б траплятись (`validity=900s` у самому інвойсі), але якщо
  трапиться — краще людина подивиться, ніж скрипт мовчки вирішить.
- **`STALE_SESSION_MINUTES = 60`** — константа з голови, як і
  `OPERATOR_SEARCH_RADIUS_KM`/`AMOUNT_PRESETS` у попередніх промптах;
  винесена як `--stale-minutes` для ручного перевизначення.
- **`list_success_payments_without_income`** робить `LEFT JOIN` на
  `operator_payout_ledger` без індексу саме під цей запит — на малому
  датасеті MVP не проблема, варто повернутись, якщо журнал виросте на
  порядки.

## Прогін тестів

```
pytest test_operator_payments.py test_operator_isolation.py -q   -> 113 passed
pytest test_reconcile_operators.py -q                             -> 16 passed
pytest -q (з BOT_TOKEN/GEMINI_API_KEY-плейсхолдерами в env)        -> 349 passed
```

Локально прогін був на Python 3.14 (єдиний у системі); цільова версія
проєкту — 3.11, її перевіряє CI. `BOT_TOKEN`/`GEMINI_API_KEY` не задані в
цьому середовищі (немає `.env`) — без них `app.core.loader` падає на
імпорті ще до збору тестів; для повного прогону вони виставлені
плейсхолдерами лише в оболонці, що запускає pytest, і ніде не збережені.
""",
    },
}


def sh(*args):
    """git-виклик у корені репозиторію; порожній рядок, якщо команда впала."""
    result = subprocess.run(
        args, cwd=ROOT, capture_output=True, text=True, encoding="utf-8"
    )
    return (result.stdout or "").strip()


def github_anchor(path: str) -> str:
    """
    Якір для заголовка виду ``## `шлях` ``: GitHub прибирає беклапки, слеші
    й крапки, ЗБЕРІГАЄ підкреслення і переводить у нижній регістр.
    """
    return path.lower().replace("/", "").replace(".", "")


# Мова підсвітки за розширенням. Раніше все загорталось у ```python — для
# Jinja2-шаблонів це давало нечитабельну кашу з підсвітки.
_FENCE_BY_SUFFIX = {
    ".py": "python",
    ".html": "html",
    ".md": "markdown",
    ".sql": "sql",
    ".sh": "bash",
    ".yml": "yaml",
    ".yaml": "yaml",
}


def fence_language(path: str) -> str:
    return _FENCE_BY_SUFFIX.get(Path(path).suffix, "text")


def build(prompt_key: str) -> Path:
    spec = PROMPTS[prompt_key]
    out = ROOT / f"review_prompt{prompt_key}.md"

    # base/tip — межі внеску саме цього промпту. Раніше бралось "останній
    # коміт HEAD", і збірка старого промпту після нових комітів показувала
    # чужий diffstat, а diff змінених файлів виходив порожнім.
    base_ref = spec.get("base", "origin/main")
    tip_ref = spec.get("tip", "HEAD")
    base = sh("git", "log", "-1", "--format=%h", base_ref)
    tip = sh("git", "log", "-1", "--format=%h %s", tip_ref)
    branch = sh("git", "rev-parse", "--abbrev-ref", "HEAD")
    diffstat = "\n".join(
        line.strip()
        for line in sh("git", "diff", "--stat", base_ref, tip_ref).splitlines()
    )

    parts = [
        f"# {spec['title']}: повний код для рев'ю\n\n"
        f"**Гілка:** `{branch}`\n"
        f"**Внесок промпту:** `{base}` → `{tip}`\n\n"
        f"{spec['intro']}\n\n"
        f"```\n{diffstat}\n```\n\n"
        "## Зміст\n\n"
    ]

    for path, status, purpose in spec["files"]:
        parts.append(f"- [`{path}`](#{github_anchor(path)}) — {status}. {purpose}\n")

    parts.append("\n" + spec["sections"] + "\n---\n")

    for path, status, purpose in spec["files"]:
        target = ROOT / path
        if not target.exists():
            raise SystemExit(f"❌ Немає файлу {path} — оновіть PROMPTS у цьому скрипті.")
        content = target.read_text(encoding="utf-8").rstrip("\n")
        lines = len(content.splitlines())
        parts.append(f"\n## `{path}`\n\n**{status}** · {lines} рядків · {purpose}\n\n")

        collapse = path in spec.get("diff_only", [])
        if collapse:
            diff = sh("git", "diff", base_ref, tip_ref, "--", path)
            if "diff --git" in diff:
                diff = diff[diff.index("diff --git"):]
            if not diff.strip():
                raise SystemExit(
                    f"❌ {path} позначений як diff_only, але між {base_ref} і "
                    f"{tip_ref} не змінювався — перевірте base/tip у PROMPTS."
                )
            parts.append(
                f"Зміна мінімальна відносно розміру файлу — сам diff:\n\n"
                f"```diff\n{diff}\n```\n\n"
                "<details>\n<summary>Повний код файлу</summary>\n\n"
            )
        parts.append(f"```{fence_language(path)}\n{content}\n```\n")
        if collapse:
            parts.append("\n</details>\n")

    out.write_text("".join(parts), encoding="utf-8")
    return out


def main():
    if len(sys.argv) != 2 or sys.argv[1] not in PROMPTS:
        available = ", ".join(sorted(PROMPTS))
        print(f"Використання: python scripts/gen_review.py <номер промпту>")
        print(f"Доступні: {available}")
        raise SystemExit(1)

    out = build(sys.argv[1])
    line_count = len(out.read_text(encoding="utf-8").splitlines())
    print(f"OK -> {out} ({line_count} рядків)")


if __name__ == "__main__":
    main()
