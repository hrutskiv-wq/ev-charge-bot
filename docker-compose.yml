version: '3.8'
services:
  bot:
    build: .
    volumes:
      - .:/app
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
  
  redis:
    image: redis:alpine
    restart: always